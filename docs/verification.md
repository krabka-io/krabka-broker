# Verification

Krabka uses two formal test tiers. Creusot proves contracts for small,
executable kernels. Stateright checks all reachable states inside each model's
stated bounds. Neither tier proves the whole broker.

## Creusot Proof Ledger

The contract attributes beside each function are the source of truth. A proof
applies only when its caller establishes every listed precondition. An empty
precondition cell means that the contract accepts all values of the Rust input
types.

| Kernel and contract | Host caller | Proof session | Caller preconditions |
| :--- | :--- | :--- | :--- |
| [`acl_identity_match`](../crates/verified/src/authz.rs), [`acl_resource_match`](../crates/verified/src/authz.rs), and [`acl_operation_match`](../crates/verified/src/authz.rs) prove principal and host wildcards, literal and prefixed resource applicability, exact operations, `All`, and only Kafka's one-way implication arrows; [`acl_decision`](../crates/verified/src/authz.rs) then applies super-user bypass, explicit deny precedence, and default deny to matching-ACL state. | The shared [`krabka-authz` applicability adapter](../crates/authz/src/simple/matching.rs) and [precedence loop](../crates/authz/src/simple.rs) | [`identity proof`](../verif/krabka_verified_rlib/authz/acl_identity_match/proof.json), [`resource proof`](../verif/krabka_verified_rlib/authz/acl_resource_match/proof.json), [`operation proof`](../verif/krabka_verified_rlib/authz/acl_operation_match/proof.json), [`precedence proof`](../verif/krabka_verified_rlib/authz/acl_decision/proof.json) | The host computes exact string equality, the canonical `User:*`, host and literal-resource `*` wildcards, `str::starts_with`, resource-type equality, and exhaustively maps every metadata operation and pattern variant. Both ACL sources must return every potentially applicable resource entry; the authorizer rechecks applicability before precedence. |
| [`request_auth_admission`](../crates/verified/src/authz.rs) admits only handshake, authentication, and version discovery before authentication, only authentication during reauthentication, and every API afterward. | The broker's shared [`ConnectionAuth` adapter](../crates/broker/src/network/auth/state.rs) | [`proof.json`](../verif/krabka_verified_rlib/authz/request_auth_admission/proof.json) | The host maps each connection phase exactly and passes the Kafka API key wire value unchanged. |
| [`spool_append_decision`](../crates/verified/src/audit.rs) admits an audit frame without overflowing the byte cap and advances the fsync cadence without wrapping. | [`krabka-audit`](../crates/audit/src/spool.rs) | [`proof.json`](../verif/krabka_verified_rlib/audit/spool_append_decision/proof.json) | `sync_every > 0`; `unsynced < sync_every`. |
| [`fetch_visibility`](../crates/verified/src/broker.rs) computes Fetch bounds and response watermarks. | [`krabka-broker`](../crates/broker/src/handlers/fetch.rs) | [`proof.json`](../verif/krabka_verified_rlib/broker/fetch_visibility/proof.json) | `0 <= log_start <= hw <= log_end`; `log_start <= deliverable <= hw`. |
| [`delete_records_target`](../crates/verified/src/broker.rs) resolves the `-1` sentinel to the high watermark. | [`krabka-broker`](../crates/broker/src/handlers/delete_records/offsets.rs) | [`proof.json`](../verif/krabka_verified_rlib/broker/delete_records_target/proof.json) | None. |
| [`delete_records_offset_out_of_range`](../crates/verified/src/broker.rs) classifies targets below zero or above the log end. | [`krabka-broker`](../crates/broker/src/handlers/delete_records/offsets.rs) | [`proof.json`](../verif/krabka_verified_rlib/broker/delete_records_offset_out_of_range/proof.json) | None. |
| [`effective_share_backlog`](../crates/verified/src/broker.rs) computes a nonnegative, saturating backlog. | [`krabka-broker`](../crates/broker/src/share_partition/backlog_poller.rs) | [`proof.json`](../verif/krabka_verified_rlib/broker/effective_share_backlog/proof.json) | None. |
| [`unclean_recovery_commit_admission`](../crates/verified/src/broker.rs) admits a polled unclean-election winner only while the partition epoch and ordered replica assignment equal the selection snapshot, the winner remains assigned, and the current leader remains unavailable. | The broker's [`UncleanRecoveryManager`](../crates/broker/src/unclean_recovery/manager.rs) post-poll commit path | [`proof.json`](../verif/krabka_verified_rlib/broker/unclean_recovery_commit_admission/proof.json) | The host captures the exact partition epoch and ordered replica IDs before polling, maps the re-read record and liveness result immediately before commit, and submits only after kernel admission. Metadata submission remains an asynchronous host boundary. |
| [`find_coordinator_admission`](../crates/verified/src/broker.rs) admits only known GROUP and TRANSACTION keys after their mapped Describe ACL succeeds, and well-formed SHARE keys at API v6+ after ClusterAction succeeds; malformed SHARE keys and unknown wire discriminants fail closed. | The broker's [`FindCoordinator` authorization adapter](../crates/broker/src/handlers/find_coordinator/authz.rs), before internal-topic creation or coordinator resolution | [`proof.json`](../verif/krabka_verified_rlib/broker/find_coordinator_admission/proof.json) | The host validates canonical SHARE composite keys and maps SHARE to `ClusterAction` on the singleton Cluster resource; only kernel allow outcomes enter resolution. |
| [`java_string_hash_partition`](../crates/verified/src/broker.rs) implements Java UTF-16 `String.hashCode`, Kafka's `Utils.abs(Integer.MIN_VALUE) = 0`, and positive partition modulo exactly. | The broker's [SHARE coordinator partitioner](../crates/broker/src/share_coordinator/partitioner.rs) | [`proof.json`](../verif/krabka_verified_rlib/broker/java_string_hash_partition/proof.json) | The host canonicalizes the topic UUID with Kafka's URL-safe unpadded encoding and passes `str::encode_utf16()` output; configuration validation requires a positive partition count. |
| [`chain_step`](../crates/verified/src/chain.rs) accepts exactly the expected sequence and opaque previous head, advances by one, and rejects sequence exhaustion instead of wrapping. | [`krabka-audit`](../crates/audit/src/verify/walk.rs); the [`krabka-remote-storage`](../crates/remote-storage/src/worm/verify/walk.rs) WORM verifier | [`proof.json`](../verif/krabka_verified_rlib/chain/chain_step/proof.json) | The host computes `head_matches` by exact comparison with its independently recomputed chain head; hashing and signatures remain outside Creusot. |
| [`select_chain_tip`](../crates/verified/src/chain.rs) selects the eligible WORM receipt with the greatest `(start offset, sequence)` rank; `chain_step` rejects writer-side sequence exhaustion instead of repeating `u64::MAX`. | The [`krabka-remote-storage`](../crates/remote-storage/src/worm/chain.rs) WORM writer and the broker [archive copy loop](../crates/broker/src/remote_log_manager/copy.rs) | [`proof.json`](../verif/krabka_verified_rlib/chain/select_chain_tip/proof.json) | The host marks a receipt eligible only after excluding delete states, decoding its metadata, and confirming that it carries a produced head; opaque heads remain outside Creusot. |
| [`compute_horizon`](../crates/verified/src/compaction.rs) adds retention time with saturation at the `i64` bounds. | `krabka-verified` calls it from `retain_decision`; [`krabka-log`](../crates/log/src/compact/decision.rs) also calls it in tests. | [`proof.json`](../verif/krabka_verified_rlib/compaction/compute_horizon/proof.json) | None. |
| [`retain_decision`](../crates/verified/src/compaction.rs) implements the KIP-534 record and control-marker retention rules. | [`krabka-log`](../crates/log/src/compact/decision.rs) | [`proof.json`](../verif/krabka_verified_rlib/compaction/retain_decision/proof.json) | None. |
| [`compaction_decode_step`](../crates/verified/src/compaction.rs) treats zero remaining input as the only clean end-of-stream and requires every successful batch decode to consume bytes. | [`krabka-log`](../crates/log/src/compact/batch_reader.rs), shared by compaction's map, transaction-survival, and rewrite passes | [`proof.json`](../verif/krabka_verified_rlib/compaction/compaction_decode_step/proof.json) | The host passes exact cursor lengths and the `RecordBatch::decode` result; corruption is surfaced before swap promotion. |
| [`retention_segment_evict`](../crates/verified/src/compaction.rs) admits time-based segment eviction only for a known timestamp whose host comparison has expired, while preserving independent size pressure. | `krabka-verified` calls it from its compaction tests. | [`proof.json`](../verif/krabka_verified_rlib/compaction/retention_segment_evict/proof.json) | None. |
| [`local_retention_prefix`](../crates/verified/src/retention.rs) selects only a contiguous oldest prefix required by age or size, stops at scheduled data, strictly reduces positive size debt when a removable segment has bytes, and reserves the final segment; [`retention_prefix`](../crates/verified/src/retention.rs) applies the corresponding finished-segment policy to tiered retention; [`retention_delete_target`](../crates/verified/src/retention.rs) advances an inclusive last offset without signed overflow. | The log's [local retention tick](../crates/log/src/log/tick.rs) and the broker's [local](../crates/broker/src/remote_log_manager/local_retention.rs) and [remote](../crates/broker/src/remote_log_manager/remote_retention.rs) KIP-405 retention walks | [`local prefix proof`](../verif/krabka_verified_rlib/retention/local_retention_prefix/proof.json), [`tiered prefix proof`](../verif/krabka_verified_rlib/retention/retention_prefix/proof.json), [`target proof`](../verif/krabka_verified_rlib/retention/retention_delete_target/proof.json) | The local host passes equal arrays in oldest-first sealed-segment order, exact segment sizes, the exact active-segment flag, an age-expired prefix from on-disk maximum timestamps, scheduled flags from a freshly scanned delivery watermark, and size debt computed from the configured `ByteSize` budget. It removes only the selected bases from memory and indexes, then attempts their required and optional files; file-delete errors leave recoverable orphan files but cannot expand the selected prefix. Tiered hosts map finished state and full-range timestamp comparisons exactly; remote candidates are already filtered and sorted. |
| [`election_jitter_ms`](../crates/verified/src/consensus.rs) keeps deterministic jitter in `[0, base_ms)`. | [`krabka-kraft-core`](../crates/kraft-core/src/core.rs) | [`proof.json`](../verif/krabka_verified_rlib/consensus/election_jitter_ms/proof.json) | None. |
| [`log_is_up_to_date`](../crates/verified/src/consensus.rs) implements the KIP-595 candidate-log ordering. | [`krabka-kraft-core`](../crates/kraft-core/src/core/vote_request.rs) | [`proof.json`](../verif/krabka_verified_rlib/consensus/log_is_up_to_date/proof.json) | None. |
| [`vote_wire_decision`](../crates/verified/src/vote.rs) rejects negative KIP-595 voter, candidate, and epoch fields; [`vote_encode_decision`](../crates/verified/src/vote.rs) rejects unsigned values above the signed wire maximum; [`vote_admission_decision`](../crates/verified/src/vote.rs) requires the exact target directory, matching cluster, and both voter memberships. | The raft [Vote codec](../crates/raft/src/kraft/transport/wire/request.rs) and kraft-core [Vote handler](../crates/kraft-core/src/core/vote_request.rs) | [`decode proof`](../verif/krabka_verified_rlib/vote/vote_wire_decision/proof.json), [`encode proof`](../verif/krabka_verified_rlib/vote/vote_encode_decision/proof.json), [`admission proof`](../verif/krabka_verified_rlib/vote/vote_admission_decision/proof.json) | The decoder also requires exactly one `__cluster_metadata` partition-0 row and full frame consumption. The host compares KIP-853 directory IDs exactly at kraft.version 1 and retains the version-0 node-ID compatibility rule; later epoch/log and double-vote checks remain in the state machine. |
| [`majority_size`](../crates/verified/src/consensus.rs) computes `floor(voters / 2) + 1`; [`election_has_quorum`](../crates/verified/src/consensus.rs) admits only that many unique grants from the current voter set. | [`krabka-kraft-core`](../crates/kraft-core/src/core/election.rs) | [`majority_size/proof.json`](../verif/krabka_verified_rlib/consensus/majority_size/proof.json), [`election_has_quorum/proof.json`](../verif/krabka_verified_rlib/consensus/election_has_quorum/proof.json) | The host filters the grant set to unique current voters before passing its count. |
| [`failover_action`](../crates/verified/src/consensus.rs) gives clean election precedence, prevents witnesses from becoming leaders, selects configured offset-aware recovery before unclean election, and otherwise shrinks or leaves the ISR unchanged. | The broker's shared [leader-failover policy](../crates/broker/src/leader_election/policy.rs) | [`proof.json`](../verif/krabka_verified_rlib/consensus/failover_action/proof.json) | The host filters live ISR members, classifies electable non-witness candidates, maps the resolved recovery strategy exactly, and constructs the selected ISR after the kernel chooses the action class. |
| [`select_best_recovery_replica`](../crates/verified/src/consensus.rs) returns an in-bounds replica whose last-written leader epoch is maximal, whose log end is maximal within that epoch, and whose broker ID is minimal on a complete tie. | The broker's KIP-966 [unclean-recovery selector](../crates/broker/src/unclean_recovery/selection.rs) | [`proof.json`](../verif/krabka_verified_rlib/consensus/select_best_recovery_replica/proof.json) | The host preserves response order while adapting each report to `(last written leader epoch, log end offset, broker ID)` and maps the proved index back to that same report. |
| [`reassignment_action`](../crates/verified/src/reassignment.rs) waits until every addition is caught up, completes immediately when the leader remains assigned, and otherwise selects the first eligible replacement leader or waits. | The broker's [partition-reassignment policy](../crates/broker/src/reassignment/policy.rs) | [`proof.json`](../verif/krabka_verified_rlib/reassignment/reassignment_action/proof.json) | The host constructs the target assignment as current replicas minus removals and marks a handoff candidate eligible exactly when it is both in the ISR and alive. |
| [`recompute_high_watermark`](../crates/verified/src/consensus.rs) computes the majority offset, applies the epoch gate, and does not regress. | [`krabka-kraft-core`](../crates/kraft-core/src/core/replication.rs); the `krabka-broker` WAL [live engine](../crates/broker/src/wal/quorum/engine/distributed.rs); the [diskless crash model](../crates/broker/src/diskless_crash_model.rs) | [`proof.json`](../verif/krabka_verified_rlib/consensus/recompute_high_watermark/proof.json) | `1 <= majority <= followers + 1`; `current_hwm <= log_end`; each follower offset is at most `log_end`. |
| [`break_glass_admission`](../crates/verified/src/break_glass.rs) applies the fail-closed withdrawn, consumed, expiry, approval, and signature checks in order; [`select_break_glass_candidate`](../crates/verified/src/break_glass.rs) deterministically chooses the earliest expiry and then lowest UUID. | The broker's break-glass [usability gate](../crates/broker/src/break_glass/gate/usability.rs) and [proposal selector](../crates/broker/src/break_glass/gate.rs) | [`admission proof`](../verif/krabka_verified_rlib/break_glass/break_glass_admission/proof.json), [`selection proof`](../verif/krabka_verified_rlib/break_glass/select_break_glass_candidate/proof.json) | The host supplies exact distinct-approval and signature facts and maps UUIDs to their canonical high and low halves. Atomic consumption and action persistence remain outside Creusot. |
| [`diskless_trim_decision`](../crates/verified/src/diskless.rs) advances local log start only behind both the committed object projection and the lagged high watermark, without subtraction overflow or regression. | The diskless WAL [object flusher](../crates/broker/src/diskless/flusher/object_flush.rs) | [`proof.json`](../verif/krabka_verified_rlib/diskless/diskless_trim_decision/proof.json) | None; negative offsets and a safety lag beyond the high watermark fail closed, while a negative configured lag preserves the host's existing zero-lag behavior. |
| [`diskless_logical_range`](../crates/verified/src/diskless.rs) selects the indexed range covering a requested offset or the immediate successor after a gap; [`diskless_span_extension`](../crates/verified/src/diskless.rs) extends only within one object across contiguous whole ranges and under the response cap; [`diskless_batch_step`](../crates/verified/src/diskless.rs) advances decoded batches without splitting them or overflowing logical or byte coordinates. | The diskless WAL cold-read [index lookup](../crates/broker/src/diskless/wal_index.rs) and [batch selector](../crates/broker/src/diskless/read.rs) | [`logical range proof`](../verif/krabka_verified_rlib/diskless/diskless_logical_range/proof.json), [`span proof`](../verif/krabka_verified_rlib/diskless/diskless_span_extension/proof.json), [`batch proof`](../verif/krabka_verified_rlib/diskless/diskless_batch_step/proof.json) | The host copies ranges in `BTreeMap` order and rejects invalid, overlapping, or unordered logical ranges before calling the selection kernel. Object-key equality is exact. Object reads, index completeness, decoded `RecordBatch` framing and CRC, and actual decode consumption remain host and I/O assumptions. Kafka permits the first returned batch to exceed the cap; every later batch must fit without splitting. |
| [`exact_epoch_successor`](../crates/verified/src/epoch.rs) returns the exact signed successor only below the caller's maximum and rejects the maximum or any value above it. | The broker's shared [metadata-epoch adapter](../crates/broker/src/metadata_epoch.rs), used by ISR updates, leader election and failover, reassignment, unclean recovery, classic and next-generation group coordination, barrier injection, share-state resets, and share snapshots | [`proof.json`](../verif/krabka_verified_rlib/epoch/exact_epoch_successor/proof.json) | None. The host passes `i32::MAX` for Kafka `int32` metadata fields and `i64::MAX` for barrier and snapshot epochs, then rejects `None` before mutation or persistence. Fetch-session and share-fetch-session epochs are excluded because their protocols intentionally wrap while skipping reserved sentinels. |
| [`barrier_cut_expired`](../crates/verified/src/retention.rs) expires a held barrier cut only when a positive retained-cut count yields a representable cutoff and the held epoch is at or below that exact cutoff. | The barrier coordinator's [retention selector](../crates/broker/src/barrier/state/retention.rs); barrier epoch allocation uses the separately proved `exact_epoch_successor` adapter above | [`proof.json`](../verif/krabka_verified_rlib/retention/barrier_cut_expired/proof.json) | Barrier retention is count-based, as persisted in `GroupValue.retained_cuts`; no timestamp, duration, or wall clock participates. The host supplies the newly published epoch, persisted count, and exact held epochs. Persistence appends tombstones for precisely the returned epochs, and recovery may replay the prior state until those records commit. |
| [`create_token_deadlines`](../crates/verified/src/delegation_token.rs), [`renew_token_expiry`](../crates/verified/src/delegation_token.rs), [`expire_token_deadline`](../crates/verified/src/delegation_token.rs), and [`token_is_active`](../crates/verified/src/delegation_token.rs) implement KIP-48 deadline selection and authentication admission without signed overflow or resurrection of expired tokens. | The broker's [create](../crates/broker/src/handlers/create_delegation_token/lifetime.rs), [renew](../crates/broker/src/handlers/renew_delegation_token.rs), [expire](../crates/broker/src/handlers/expire_delegation_token.rs), and [SCRAM](../crates/broker/src/network/auth/scram.rs) paths | [`create proof`](../verif/krabka_verified_rlib/delegation_token/create_token_deadlines/proof.json), [`renew proof`](../verif/krabka_verified_rlib/delegation_token/renew_token_expiry/proof.json), [`expire proof`](../verif/krabka_verified_rlib/delegation_token/expire_token_deadline/proof.json), [`authentication proof`](../verif/krabka_verified_rlib/delegation_token/token_is_active/proof.json) | Owner/renewer authorization and controller persistence remain in the host handlers; renew/expire still depend on an image snapshot followed by an unconditional controller write. |
| [`token_api_admission`](../crates/verified/src/delegation_token.rs) requires a non-anonymous authenticated identity for every token API and permits token-authenticated sessions to describe, but never create, renew, or expire, tokens. | The broker's shared [`ConnectionAuth` adapter](../crates/broker/src/network/auth/state.rs), called by all four delegation-token handlers before token access or mutation | [`proof.json`](../verif/krabka_verified_rlib/delegation_token/token_api_admission/proof.json) | The host maps only `AuthMethod::Anonymous` and unestablished connection states to an absent authenticated identity. |
| [`token_describe_visible`](../crates/verified/src/delegation_token.rs) restricts token-authenticated callers to tokens they own and otherwise requires the owner filter plus an owner, renewer, or Describe-ACL relationship. | The broker's [`DescribeDelegationToken` filter](../crates/broker/src/handlers/describe_delegation_token.rs) | [`proof.json`](../verif/krabka_verified_rlib/delegation_token/token_describe_visible/proof.json) | The host compares canonical principals, treats a missing or empty wire owner list as no filter, and passes the authorizer's exact `TOKEN:<owner>` Describe result. |
| [`scram_credential_source`](../crates/verified/src/delegation_token.rs) always prefers a regular SCRAM credential, permits delegation-token fallback only for SCRAM-SHA-256, distinguishes expired tokens, and fails unknown users closed. | The broker's [SCRAM round-one credential lookup](../crates/broker/src/network/auth/scram.rs) | [`proof.json`](../verif/krabka_verified_rlib/delegation_token/scram_credential_source/proof.json) | The host performs exact metadata lookups and derives token activity through the separately proved `token_is_active` kernel before synthesizing a KIP-48 credential. |
| [`handoff_high_watermark`](../crates/verified/src/consensus.rs) returns the maximum of the old and new frontiers. | No host caller. | [`proof.json`](../verif/krabka_verified_rlib/consensus/handoff_high_watermark/proof.json) | None. |
| [`epoch_and_offset_for_entries`](../crates/verified/src/leader_epoch.rs) returns the KIP-101 leader epoch and offset where a follower truncates its divergent suffix. | [`krabka-log`](../crates/log/src/leader_epoch_checkpoint/lookup.rs) | [`proof.json`](../verif/krabka_verified_rlib/leader_epoch/epoch_and_offset_for_entries/proof.json) | Entries have strictly increasing leader epochs and start offsets. |
| [`offset_index_lookup`](../crates/verified/src/log_index.rs) returns the position for the greatest relative offset at or below the target. | [`krabka-log`](../crates/log/src/index/offset.rs) | [`proof.json`](../verif/krabka_verified_rlib/log_index/offset_index_lookup/proof.json) | Entries have strictly increasing relative offsets. |
| [`offset_index_position_at_or_after`](../crates/verified/src/log_index.rs) returns the position for the least relative offset at or above the target. | [`krabka-log`](../crates/log/src/index/offset.rs) | [`proof.json`](../verif/krabka_verified_rlib/log_index/offset_index_position_at_or_after/proof.json) | Entries have strictly increasing relative offsets. |
| [`time_index_lookup`](../crates/verified/src/log_index.rs) returns the relative offset for the last timestamp at or below the target. | [`krabka-log`](../crates/log/src/index/time.rs) | [`proof.json`](../verif/krabka_verified_rlib/log_index/time_index_lookup/proof.json) | Entries have nondecreasing timestamps. |
| [`isr_high_watermark`](../crates/verified/src/isr.rs) computes the minimum log end represented by a classic Kafka leader and its ISR followers. | [`krabka-broker`](../crates/broker/src/replica_state.rs) | [`proof.json`](../verif/krabka_verified_rlib/isr/isr_high_watermark/proof.json) | The host identifies the current leader, includes every other ISR member, and substitutes zero for missing follower progress during the leadership-change reseed window. |
| [`isr_maintenance_selected`](../crates/verified/src/isr.rs) retains an assigned leader, retains assigned ISR followers only while fetch-recent, and admits assigned non-ISR replicas only when both fetch-recent and recently caught up; [`isr_proposal_changed`](../crates/verified/src/isr.rs) suppresses exactly proposals with no unique additions or removals. | The broker's ISR maintenance [proposal builder](../crates/broker/src/isr_maintenance/proposal.rs) | [`selection proof`](../verif/krabka_verified_rlib/isr/isr_maintenance_selected/proof.json), [`no-op proof`](../verif/krabka_verified_rlib/isr/isr_proposal_changed/proof.json) | The replica-state installation retains the exact metadata assignment and leader. The host makes one decision per sorted `BTreeSet` candidate, so output is unique and sorted. `Instant` is monotonic; `last_fetch` records liveness, and `last_caught_up` is refreshed only by a fetch whose follower LEO reached the observed leader LEO. The scan's single timestamp and configured lag bound supply both recency facts. |
| [`isr_admission`](../crates/verified/src/isr.rs) applies AlterPartition's ordered fail-closed checks: leader-epoch fencing, non-empty valid replica subset, broker-epoch eligibility, then admission. | The broker's [`AlterPartition` row adapter](../crates/broker/src/handlers/alter_partition/isr_update.rs) | [`proof.json`](../verif/krabka_verified_rlib/isr/isr_admission/proof.json) | The host rejects negative wire broker IDs, checks every proposed member against the partition replicas, and validates every supplied non-sentinel broker epoch against the current registration. |
| [`reserve_offsets`](../crates/verified/src/offset_allocator.rs) returns the base and advances the next offset by the count. | [`krabka-raft`](../crates/raft/src/kraft/controller/submit.rs) | [`proof.json`](../verif/krabka_verified_rlib/offset_allocator/reserve_offsets/proof.json) | `count >= 0`; `next + count <= i64::MAX`. |
| [`producer_decision`](../crates/verified/src/producer.rs) classifies an idempotent-producer batch as append, duplicate, out of order, or fenced. | [`krabka-broker`](../crates/broker/src/producer_state/decision.rs); [`increment_sequence`](../crates/verified/src/producer.rs) is also called by [`krabka-log`](../crates/log/src/log/open.rs). | [`proof.json`](../verif/krabka_verified_rlib/producer/producer_decision/proof.json) | None. |
| [`produce_batch_admission`](../crates/verified/src/produce.rs) accepts exactly a nonempty data batch with base offset zero, consistent positive record count, a valid producer sequence when idempotent, and CreateTime timestamps; malformed headers take precedence over timestamp rejection. | The broker's [Produce batch preparation](../crates/broker/src/handlers/produce/prepare.rs) | [`proof.json`](../verif/krabka_verified_rlib/produce/produce_batch_admission/proof.json) | CRC validation runs first; the host maps the decoded control and timestamp-type attributes exactly. |
| [`advance_high_watermark`](../crates/verified/src/raft.rs) advances a KRaft HWM monotonically without passing log end; [`in_half_open_window`](../crates/verified/src/raft.rs) and [`frontier_reaches`](../crates/verified/src/raft.rs) define commit, fetch, apply, and waiter boundaries. | [`krabka-raft`](../crates/raft/src/kraft/log.rs) and its controller [offset rules](../crates/raft/src/kraft/controller/offsets.rs) | [`advance_high_watermark/proof.json`](../verif/krabka_verified_rlib/raft/advance_high_watermark/proof.json), [`in_half_open_window/proof.json`](../verif/krabka_verified_rlib/raft/in_half_open_window/proof.json), [`frontier_reaches/proof.json`](../verif/krabka_verified_rlib/raft/frontier_reaches/proof.json) | The HWM bound requires the host invariant `previous <= log_end`; the predicates accept all integers. |
| [`control_history_frontier`](../crates/verified/src/raft.rs) returns the exact prefix of strictly ordered control-record offsets below a half-open frontier, so the prefix excludes a record exactly at the frontier and its suffix contains every record at or above it. | The KRaft controller's voter and feature [control history](../crates/raft/src/kraft/controller/control_state.rs), for point lookup, truncation, and commitment | [`proof.json`](../verif/krabka_verified_rlib/raft/control_history_frontier/proof.json) | The host copies keys from a `BTreeMap`, whose unique ascending iteration establishes the kernel's strict-order precondition, and maps the proved last prefix key back into that same map. Truncation splits at the proved first suffix key. Commitment is called with the controller's monotonic HWM and selects only the latest record strictly below it. |
| [`metadata_record_coordinates`](../crates/verified/src/raft.rs) assigns each nonempty metadata batch the exact contiguous record deltas `0..n-1`, rejects indexes outside the batch, and rejects counts whose final delta does not fit Kafka's signed field. | The KRaft controller's [`metadata_record_batch`](../crates/raft/src/kraft/controller/records.rs) builder | [`proof.json`](../verif/krabka_verified_rlib/raft/metadata_record_coordinates/proof.json) | The host passes the exact slice length and each `enumerate` index; an empty slice produces an empty batch with delta zero, and any coordinate rejection occurs before append. |
| [`fetch_response_mutation`](../crates/verified/src/raft.rs) rejects a KRaft Fetch response unless its sender, advertised leader, current leader, live fetching role, and epoch all agree, then selects exactly one of snapshot, truncation, append, or HWM advancement. | The KRaft controller's [`on_fetch_response`](../crates/raft/src/kraft/controller/replication.rs) adapter, immediately after wire decode and before peer discovery or response-derived mutation | [`proof.json`](../verif/krabka_verified_rlib/raft/fetch_response_mutation/proof.json) | The host maps `Follower` and an `Observer` with a known leader to the role-leader input, maps every other role to `None`, and passes the decoded sender, leader, epoch, and mutation-presence flags unchanged. Wire decoding itself is outside Creusot. |
| [`voter_reconfiguration_decision`](../crates/verified/src/reconfiguration.rs) admits one KIP-853 add, remove, update, or `kraft.version` finalization only for a current-epoch leader with no active change; it preserves a nonempty voter set and constructs the exact next count, version, and control-record shape. | The KRaft controller's [`on_reconfigure`](../crates/raft/src/kraft/controller/reconfiguration.rs) adapter before any voter-state mutation or control-record append | [`proof.json`](../verif/krabka_verified_rlib/reconfiguration/voter_reconfiguration_decision/proof.json) | The host takes membership and `kraft.version` from one committed control-state snapshot, requires the latest state to equal it and no pending waiter, and maps epoch commitment from `HWM > epoch_start_offset`. It compares removal directory IDs exactly; update also accepts the legacy nil directory only during version-0 preflight. Version ranges come unchanged from decoded voter metadata. Observer catch-up uses the latest recorded fetch offset against the current log end; that liveness observation can become stale after admission, while safety still depends on the current quorum committing the appended voter record. The host checks the proved next count against the constructed set. |
| [`replay_record_decision`](../crates/verified/src/recovery.rs) applies only records with overflow-safe offsets inside `[from, high watermark)` and separates control from data replay; [`replay_cursor_decision`](../crates/verified/src/recovery.rs) advances only on strict progress; [`replay_batch_cursor_decision`](../crates/verified/src/recovery.rs) accepts a decoded batch only when its base does not overlap the cursor and its exclusive successor is representable, strictly advancing, and at or below the captured end; [`should_capture_first_downgrade`](../crates/verified/src/recovery.rs) retains only the first downgrade. | The KRaft controller's [committed data, control-state, and control-record recovery walks](../crates/raft/src/kraft/controller/recovery.rs), the group coordinator's [`__consumer_offsets` replay](../crates/broker/src/coordinator/bootstrap/replay.rs), and the barrier coordinator's [`__barrier_state` replay](../crates/broker/src/barrier/coordinator/recovery.rs) | [`record proof`](../verif/krabka_verified_rlib/recovery/replay_record_decision/proof.json), [`cursor proof`](../verif/krabka_verified_rlib/recovery/replay_cursor_decision/proof.json), [`batch cursor proof`](../verif/krabka_verified_rlib/recovery/replay_batch_cursor_decision/proof.json), [`downgrade proof`](../verif/krabka_verified_rlib/recovery/should_capture_first_downgrade/proof.json) | Batch framing, CRC checking, and record/control decoding remain in the host. Each caller passes the decoded batch base and signed last delta unchanged, threads the admitted exclusive end into the next call, and captures the log end before replay. Kafka compaction may leave forward gaps, which the kernel permits. The group coordinator returns a startup error for a rejected batch; barrier recovery warns and stops that partition before applying the rejected batch. |
| [`remote_metadata_partition`](../crates/verified/src/remote_metadata.rs) implements Kafka's KIP-405 metadata partitioner exactly: Java UUID-half hashing, big-endian `int` serialization, Murmur2, positive masking, and modulo; nonpositive partition counts are rejected. | [`krabka-remote-storage-topic`](../crates/remote-storage-topic/src/partitioning.rs) | [`proof.json`](../verif/krabka_verified_rlib/remote_metadata/remote_metadata_partition/proof.json) | `Uuid::as_u128` preserves the canonical most-significant and least-significant 64-bit images consumed by the kernel. |
| [`remote_read_relative_offset`](../crates/verified/src/remote_read.rs) admits only finished remote segments whose inclusive range contains the request, whose requested leader epoch owns that exact `[epoch start, next epoch start)` subrange, and whose relative offset fits the sparse index. | The remote-storage [metadata cache](../crates/remote-storage/src/cache.rs) and broker [remote Fetch reader](../crates/broker/src/remote_reader/fetch.rs) | [`proof.json`](../verif/krabka_verified_rlib/remote_read/remote_read_relative_offset/proof.json) | The host derives the requested and next epoch starts from each segment's ordered epoch map; every primary and fallback candidate delegates before index or object access. |
| [`remote_fetch_end_position`](../crates/verified/src/remote_read.rs) computes an inclusive bounded-fetch end without wrapping and uses an open end for zero, exact-segment, or oversized caps. | The remote-storage [`OffsetIndex` range adapter](../crates/remote-storage/src/index/offset.rs) | [`proof.json`](../verif/krabka_verified_rlib/remote_read/remote_fetch_end_position/proof.json) | The host saturates `usize` caps above `u32::MAX` before delegation, which preserves the open-ended result. |
| [`remote_time_index_offset_usable`](../crates/verified/src/remote_read.rs) stops decoding at the first non-increasing relative offset; [`remote_time_index_candidate_count`](../crates/verified/src/remote_read.rs) selects the maximal usable prefix strictly below a target timestamp. | The remote-storage [`TimeIndex` floor adapter](../crates/remote-storage/src/index/time.rs) | [`padding proof`](../verif/krabka_verified_rlib/remote_read/remote_time_index_offset_usable/proof.json), [`selection proof`](../verif/krabka_verified_rlib/remote_read/remote_time_index_candidate_count/proof.json) | The zero-copy parser exposes only complete 12-byte big-endian entries and ignores a trailing partial entry. The adapter decodes each relative offset, delegates the padding decision, and does not decode the terminator's timestamp or inspect later entries. It then maps every accepted timestamp and relative offset exactly. Kafka time-index writers produce nondecreasing timestamps while usable relative offsets strictly increase. The proof covers the decoded sequence after this parser boundary. |
| [`remote_txn_overlap_decision`](../crates/verified/src/remote_txn.rs) classifies inclusive aborted-transaction and Fetch ranges exactly, rejecting either inverted interval. | The remote-storage [transaction-index reader](../crates/remote-storage/src/index/txn.rs), which also rejects payloads not divisible by the 24-byte entry size | [`proof.json`](../verif/krabka_verified_rlib/remote_txn/remote_txn_overlap_decision/proof.json) | The host decodes each big-endian entry field exactly and rejects a torn partial entry before calling the kernel. |
| [`restore_batch_step`](../crates/verified/src/restore.rs) accepts only a non-overlapping archived batch base and computes its exclusive end without overflow, while permitting gaps preserved by Kafka compaction; [`restore_record_coordinates`](../crates/verified/src/restore.rs) validates each record delta evaluated by an applicable bound/filter predicate and derives its absolute offset and timestamp without overflow. | The restore verifier's [record-batch walk](../crates/restore/src/verify/log_walk.rs) and the materializer's [bound/filter path](../crates/restore/src/materialize/prepare.rs) | [`batch proof`](../verif/krabka_verified_rlib/restore/restore_batch_step/proof.json), [`record proof`](../verif/krabka_verified_rlib/restore/restore_record_coordinates/proof.json) | Batch framing and CRC are validated first; record parsing and coordinate failures in the filtering path are surfaced as archive-integrity errors before selection or rewriting. |
| [`schema_failure_decision`](../crates/verified/src/schema.rs) permits unvalidated records only when fail-open is configured and the registry failure is transient. | The broker schema validator's [registry classifier](../crates/broker/src/schema_validation/validator/cache.rs) and [admission path](../crates/broker/src/schema_validation/validator/check.rs) | [`proof.json`](../verif/krabka_verified_rlib/schema/schema_failure_decision/proof.json) | The host classifies transport errors, HTTP 408/429, and 5xx as transient; unknown IDs, other statuses, and malformed successful responses fail closed. |
| [`snapshot_chunk_admission`](../crates/verified/src/snapshot.rs) admits chunks only for one fixed snapshot identity and declared size, at the exact accumulated byte position, within the configured bound; it completes only when the next position equals the declared size and rejects overflow, duplicates, gaps, overlaps, and changed metadata without advancing. | KRaft's follower [`SnapshotFetchState`](../crates/kraft-core/src/snapshot_fetch.rs) | [`proof.json`](../verif/krabka_verified_rlib/snapshot/snapshot_chunk_admission/proof.json) | The host compares the full `(end offset, epoch)` identity, converts exact buffer and chunk lengths to signed wire coordinates, and mutates the latched size and byte buffer only after admission. `BytesMut` allocation and copy success, plus the engine's documented discard of state after `Restart`, remain host and file-I/O assumptions. |
| [`share_prune_frontier`](../crates/verified/src/share.rs) returns the minimum latest-snapshot offset across every supplied live share key, proving both lower-bound safety and the maximal safe prune point. | The share coordinator's [`__share_group_state` pruning adapter](../crates/broker/src/share_coordinator/pruning.rs) | [`proof.json`](../verif/krabka_verified_rlib/share/share_prune_frontier/proof.json) | The caller freezes every live key mapped to one state partition, then supplies each exact `last_snapshot_offset`. A key without a folded snapshot carries the default offset `0`, which pins pruning at the log start. An empty partition returns no frontier. Concurrent state changes after collection may make pruning more conservative; the selected lower bound remains safe. |
| [`site_loss_survivors`](../crates/verified/src/stretch.rs) computes the replica count after one site fails. | `krabka-verified` calls it from `min_insync_is_site_loss_safe`; the [`krabka-broker` stretch model](../crates/broker/src/stretch_cluster_model/config.rs) calls it directly. | [`proof.json`](../verif/krabka_verified_rlib/stretch/site_loss_survivors/proof.json) | `1 <= rf <= 1024`; `1 <= sites <= 1024`. |
| [`min_insync_is_site_loss_safe`](../crates/verified/src/stretch.rs) checks the lower and upper durability bounds for `min.insync.replicas`. | [`krabka-broker`](../crates/broker/src/config/stretch.rs) | [`proof.json`](../verif/krabka_verified_rlib/stretch/min_insync_is_site_loss_safe/proof.json) | `1 <= rf <= 1024`; `1 <= sites <= 1024`. |
| [`quorum_survives_any_single_site_loss`](../crates/verified/src/stretch.rs) checks that each one-site loss leaves a strict voter majority. | The [`krabka-broker` stretch model](../crates/broker/src/stretch_cluster_model/config.rs); no production caller. | [`proof.json`](../verif/krabka_verified_rlib/stretch/quorum_survives_any_single_site_loss/proof.json) | At most 1024 sites; each site has between 0 and 1024 voters. |
| [`plan_consume`](../crates/verified/src/throttle.rs) caps a refill, grants at most the request, and conserves the capped tokens. | [`krabka-throttle`](../crates/throttle/src/runtime/consume.rs) | [`proof.json`](../verif/krabka_verified_rlib/throttle/plan_consume/proof.json) | None. |
| [`transaction_completion_decision`](../crates/verified/src/transaction.rs) permits `EndTxn` completion only for the prepared producer identity and state, treats only the intended completed identity as idempotent, and rejects stale or conflicting state. | The broker's [`EndTxn` reacquisition guard](../crates/broker/src/txn/handlers/end_txn/reacquire.rs) and [completion decision](../crates/broker/src/txn/decision.rs) | [`proof.json`](../verif/krabka_verified_rlib/transaction/transaction_completion_decision/proof.json) | Prepare and complete state tags are distinct; the host maps Kafka transaction states and producer identities exactly into primitives. |
| [`next_producer_identity`](../crates/verified/src/transaction.rs) preserves unverified identities, increments normal and recovery epochs within their distinct bounds, and rotates to a fresh producer ID at the fencing boundary. | The broker's [`EndTxn` producer-identity adapter](../crates/broker/src/txn/handlers/end_txn/producer_identity.rs) | [`proof.json`](../verif/krabka_verified_rlib/transaction/next_producer_identity/proof.json) | The host selects recovery mode only for staged recovery identities and supplies a fresh producer ID when rotation is required. |
| [`resolve_transaction_timeout`](../crates/verified/src/transaction.rs) maps KIP-939 transactions to the non-colliding `i32::MAX` sentinel and otherwise clamps the requested timeout; [`should_abort_idle_transaction`](../crates/verified/src/transaction.rs) permits the reaper to abort only an expired, ongoing, non-2PC transaction and fails closed on a backwards clock. | The broker's shared [2PC timeout and reaper adapter](../crates/broker/src/txn/two_pc.rs) | [`timeout proof`](../verif/krabka_verified_rlib/transaction/resolve_transaction_timeout/proof.json), [`reaper proof`](../verif/krabka_verified_rlib/transaction/should_abort_idle_transaction/proof.json) | Configuration establishes `0 < min <= max < i32::MAX`; the host maps only `TxnState::Ongoing` to the kernel's ongoing state. |
| [`future_log_swap_admission`](../crates/verified/src/storage.rs) permits a future-log swap only when the current and future log-end offsets are exactly equal, preventing both truncation and unreplicated suffix promotion. | The broker's [future-log swap guard](../crates/broker/src/partition_writer/swap.rs) | [`proof.json`](../verif/krabka_verified_rlib/storage/future_log_swap_admission/proof.json) | The host holds both log locks and supplies their exact log-end offsets. |
| [`remote_segment_transition`](../crates/verified/src/storage.rs) and [`remote_partition_delete_transition`](../crates/verified/src/storage.rs) admit exactly the legal remote-segment and remote-partition deletion lifecycle edges, including their terminal states. | The remote-storage [segment state](../crates/remote-storage/src/metadata/segment_state.rs) and [partition-delete state](../crates/remote-storage/src/metadata/partition_delete.rs) adapters | [`segment proof`](../verif/krabka_verified_rlib/storage/remote_segment_transition/proof.json), [`partition-delete proof`](../verif/krabka_verified_rlib/storage/remote_partition_delete_transition/proof.json) | The host exhaustively maps each Rust enum variant to the proof tags documented by the kernels; `0` means `CopySegmentStarted` for segments and no prior state for partition deletion. Exhaustive production tests cover every source and destination pair. |
| [`wal_fetch_admission`](../crates/verified/src/wal.rs) fails closed unless authenticated and claimed nodes agree, the local node leads the placement, the claimant is a voter, and the requested epoch is current or unset. | [`krabka-broker`](../crates/broker/src/wal/quorum/registry.rs) | [`proof.json`](../verif/krabka_verified_rlib/wal/wal_fetch_admission/proof.json) | The host maps the authenticated identity and current placement exactly into primitive node IDs and voter order. |
| [`exact_wal_batch_range`](../crates/verified/src/wal.rs) accepts only an empty exact range or decoded batches whose first base equals the requested start, every checked `last + 1` equals the next base, and final successor equals the target; gaps, overlaps, reordering, inverted batches, and successor overflow fail closed. | The diskless WAL [`exact_batches`](../crates/broker/src/wal/quorum/engine/batches.rs) gate used by source and replica readers | [`proof.json`](../verif/krabka_verified_rlib/wal/exact_wal_batch_range/proof.json) | The host decodes and validates each record batch first, then passes parallel base/last arrays in the original byte order plus the exact requested half-open bounds. |
| [`scheduled_delivery_visible`](../crates/verified/src/delivery.rs) exposes a scheduled batch only when its checked activation deadline is at or before the broker clock and fails closed on invalid uncertainty or overflow; [`delivery_watermark_advance`](../crates/verified/src/delivery.rs) clamps the cached watermark into live log bounds, then advances it monotonically without passing the log end; [`coalesce_delivery_range`](../crates/verified/src/delivery.rs) merges exactly overlapping or adjacent inclusive ranges and returns their maximum end without successor overflow. | The log's [local activation walk and watermark](../crates/log/src/log/delivery.rs), [range fold](../crates/log/src/delivery.rs), and the broker's [remote fetch gate](../crates/broker/src/handlers/fetch/remote.rs) | [`visibility proof`](../verif/krabka_verified_rlib/delivery/scheduled_delivery_visible/proof.json), [`watermark proof`](../verif/krabka_verified_rlib/delivery/delivery_watermark_advance/proof.json), [`coalescing proof`](../verif/krabka_verified_rlib/delivery/coalesce_delivery_range/proof.json) | Configuration supplies a nonnegative millisecond clock-uncertainty bound; local and remote paths use the same broker clock reading and decoded batch `max_timestamp`. Storage supplies valid ordered batch ranges and `log_start <= log_end`. Retention or truncation may first clamp a stale watermark down to the new live range; without a range change, each proved step is monotonic. |
| [`select_wal_voter_index`](../crates/verified/src/wal.rs) returns the first sorted candidate whose node and rack are both unused, additionally requiring the local node for the first placement. | The broker's [diskless WAL placement policy](../crates/broker/src/wal/quorum/placement.rs) | [`proof.json`](../verif/krabka_verified_rlib/wal/select_wal_voter_index/proof.json) | The host sorts brokers by node ID, maps exact rack-string equality to compact IDs, and maps the proved index back to the same candidate list. |

Creusot also creates sessions for logic functions, lemmas, private helper
functions, and generated `Clone` implementations. These sessions support the
contracts in the ledger. They are not additional production kernels.

## Run the Creusot Proofs

The [`.creusot-version`](../.creusot-version) file pins the Creusot release.
That release pins the Rust nightly that `creusot-rustc` needs. Its
`creusot-setup/src/tools_versions_urls.rs` file pins the prover binaries and
checksums. Its `creusot-deps.opam` file pins Why3 and Why3find. The local
[`why3find.json`](../why3find.json) file selects the provers and sets the time
limits, search depth, tactics, and proof-search profile. It does not set a
memory limit.

Install the pinned Creusot release and its provers as the
[`proofs` job](../.github/workflows/ci.yml) does. Then run these commands from
the repository root:

```sh
export PATH="${HOME}/.local/share/creusot/bin:${PATH}"
python3 tools/test_creusot_packages.py
packages=(krabka-verified)
python3 tools/creusot_packages.py "${packages[@]}"
for package in "${packages[@]}"; do
  cargo creusot --package "${package}"
done
```

The package check fails if a workspace package that depends on `creusot-std`
or contains a Creusot contract is absent from the explicit proof list. Each
`cargo creusot` command updates its package directory under `verif/`. A
nonzero exit or an unproved goal fails the CI job. CI checks the proof result,
not a clean Git diff. Generated `.coma` files contain an absolute source path,
so another checkout can change them without a change to the proof result.

## Stateright Model-Check Tier

Stateright enumerates reachable states within explicit bounds. It can find a
counterexample in that state space. It does not prove behavior outside the
bounds. Each model states its bounds and properties in its module comments.

The current model entry points are:

| Area | Model entry points |
| :--- | :--- |
| Broker data and Fetch | [`data_path_model`](../crates/broker/src/data_path_model.rs), [`producer_state_model`](../crates/broker/src/producer_state_model.rs), [`replica_state_model`](../crates/broker/src/replica_state_model.rs), [`fetch_session_model`](../crates/broker/src/fetch_session_model.rs), and [`fetch_visibility_model`](../crates/broker/src/handlers/fetch_visibility_model.rs). |
| Broker failover and placement | [`leader_failover_model`](../crates/broker/src/leader_failover_model.rs), [`reassignment_model`](../crates/broker/src/reassignment_model.rs), [`stretch_cluster_model`](../crates/broker/src/stretch_cluster_model.rs), [`diskless_crash_model`](../crates/broker/src/diskless_crash_model.rs), and [`client_server_failover_model`](../crates/broker/src/client_server_failover_model.rs). |
| Broker groups and transactions | [`classic_state_model`](../crates/broker/src/coordinator/unified/classic_state_model.rs), [`consumer_group_composition_model`](../crates/broker/src/coordinator/unified/consumer_group_composition_model.rs), [`reconciler_model`](../crates/broker/src/coordinator/unified/reconciler_model.rs), [`decision_model`](../crates/broker/src/txn/decision_model.rs), [`eos_composition_model`](../crates/broker/src/txn/eos_composition_model.rs), and [`two_pc_model`](../crates/broker/src/txn/two_pc_model.rs). |
| Broker share and break-glass | [`share_partition/state_model`](../crates/broker/src/share_partition/state_model.rs), [`break_glass/state_model`](../crates/broker/src/break_glass/state_model.rs), and [`break_glass/cross_spend_model`](../crates/broker/src/break_glass/cross_spend_model.rs). |
| Log | [`compact_model`](../crates/log/src/compact_model.rs) and [`leader_epoch_model`](../crates/log/src/leader_epoch_model.rs). |
| KRaft | [`kraft_model`](../crates/raft/tests/kraft_model.rs). |
| Throttle | [`bucket_model`](../crates/throttle/tests/bucket_model.rs). |

Some models drive a production kernel directly. Other models compose real
decision functions with a small environment. Read the model's `DRIVEN` and
`MODELED` notes before you treat its property as a production guarantee.

## Outside Both Tiers

Code that is not in the Creusot ledger or the Stateright inventory is in
neither tier. This includes most protocol encoding and decoding, authorization,
network and async orchestration, file and object-store I/O, and operational
configuration. Unit, property, integration, differential, and container tests
cover those areas. Those tests are evidence, but they are not a Creusot proof
or a Stateright model check.

A proved kernel does not prove its caller. The proof starts after the caller
establishes the preconditions in the ledger. A Stateright model does not prove
the I/O around the decisions that it drives.
