# Verification

Krabka uses two formal test tiers. Creusot proves contracts for small,
executable kernels. Stateright checks all reachable states inside each model's
stated bounds. Neither tier proves the whole broker.

## Creusot Proof Ledger

The contract attributes beside each function are the source of truth. A proof
applies only when its caller establishes every listed precondition. An empty
precondition cell means that the contract accepts all values of the Rust input
types.

| Kernel and contract | Host caller | Proof session | Caller preconditions |
| :--- | :--- | :--- | :--- |
| [`acl_identity_match`](../crates/verified/src/authz.rs), [`acl_resource_match`](../crates/verified/src/authz.rs), and [`acl_operation_match`](../crates/verified/src/authz.rs) prove principal and host wildcards, literal and prefixed resource applicability, exact operations, `All`, and only Kafka's one-way implication arrows; [`acl_decision`](../crates/verified/src/authz.rs) then applies super-user bypass, explicit deny precedence, and default deny to matching-ACL state. | The shared [`krabka-authz` applicability adapter](../crates/authz/src/simple/matching.rs) and [precedence loop](../crates/authz/src/simple.rs) | [`identity proof`](../verif/krabka_verified_rlib/authz/acl_identity_match/proof.json), [`resource proof`](../verif/krabka_verified_rlib/authz/acl_resource_match/proof.json), [`operation proof`](../verif/krabka_verified_rlib/authz/acl_operation_match/proof.json), [`precedence proof`](../verif/krabka_verified_rlib/authz/acl_decision/proof.json) | The host computes exact string equality, the canonical `User:*`, host and literal-resource `*` wildcards, `str::starts_with`, resource-type equality, and exhaustively maps every metadata operation and pattern variant. Both ACL sources must return every potentially applicable resource entry; the authorizer rechecks applicability before precedence. |
| [`request_auth_admission`](../crates/verified/src/authz.rs) admits only handshake, authentication, and version discovery before authentication, only authentication during reauthentication, and every API afterward. | The broker's shared [`ConnectionAuth` adapter](../crates/broker/src/network/auth/state.rs) | [`proof.json`](../verif/krabka_verified_rlib/authz/request_auth_admission/proof.json) | The host maps each connection phase exactly and passes the Kafka API key wire value unchanged. |
| [`spool_append_decision`](../crates/verified/src/audit.rs) admits an audit frame without overflowing the byte cap and advances the fsync cadence without wrapping. | [`krabka-audit`](../crates/audit/src/spool.rs) | [`proof.json`](../verif/krabka_verified_rlib/audit/spool_append_decision/proof.json) | `sync_every > 0`; `unsynced < sync_every`. |
| [`fetch_visibility`](../crates/verified/src/broker.rs) computes Fetch bounds and response watermarks. | [`krabka-broker`](../crates/broker/src/handlers/fetch.rs) | [`proof.json`](../verif/krabka_verified_rlib/broker/fetch_visibility/proof.json) | `0 <= log_start <= hw <= log_end`; `log_start <= deliverable <= hw`. |
| [`replica_fetch_mutation`](../crates/verified/src/broker.rs) rejects a follower Fetch response unless its unique topic and partition identity, in-flight request epoch, current metadata target, and any reported leader identity all agree, then selects exactly one of retry, truncation, or append. | The broker follower replicator's [Fetch loop](../crates/broker/src/replicator/fetch_loop.rs) and [response adapter](../crates/broker/src/replicator/response.rs), before any response-derived mutation | [`proof.json`](../verif/krabka_verified_rlib/broker/replica_fetch_mutation/proof.json) | The host captures the exact request epoch before sending; accepts exactly one response row whose every populated topic identity field and partition index match; accepts `current_leader` only when both fields are absent sentinels or both exactly match the configured target; and rechecks metadata plus the local replication-target lock before truncation or append. Wire decoding, record-batch validation, filesystem I/O, producer-state updates, metrics, and separately guarded error recovery remain host assumptions. |
| [`preferred_rebalance_admission`](../crates/verified/src/broker.rs) admits only a nonempty, unique, eligible preferred-leader change set whose count is within the scanned partition total and whose exact threshold comparison succeeded. | The broker's KIP-460 [preferred-leader rebalance tick](../crates/broker/src/leader_rebalance.rs), immediately before controller submission | [`proof.json`](../verif/krabka_verified_rlib/broker/preferred_rebalance_admission/proof.json) | A successful preferred-election selection supplies the live, in-ISR, non-witness eligibility fact for the scan snapshot. The host rejects duplicate topic-partition keys and compares `selected / total` with the configured ratio's shortest exact decimal fraction by a division-only continued-fraction walk, avoiding truncation, lossy count conversion, and multiplication overflow. Liveness can change after the asynchronous check, and controller submission and metadata-image concurrency remain host assumptions; a later tick retries a failed submission. |
| [`delete_records_target`](../crates/verified/src/broker.rs) resolves the `-1` sentinel to the high watermark. | [`krabka-broker`](../crates/broker/src/handlers/delete_records/offsets.rs) | [`proof.json`](../verif/krabka_verified_rlib/broker/delete_records_target/proof.json) | None. |
| [`delete_records_offset_out_of_range`](../crates/verified/src/broker.rs) classifies targets below zero or above the log end. | [`krabka-broker`](../crates/broker/src/handlers/delete_records/offsets.rs) | [`proof.json`](../verif/krabka_verified_rlib/broker/delete_records_offset_out_of_range/proof.json) | None. |
| [`effective_share_backlog`](../crates/verified/src/broker.rs) computes a nonnegative, saturating backlog. | [`krabka-broker`](../crates/broker/src/share_partition/backlog_poller.rs) | [`proof.json`](../verif/krabka_verified_rlib/broker/effective_share_backlog/proof.json) | None. |
| [`unclean_recovery_commit_admission`](../crates/verified/src/broker.rs) admits a polled unclean-election winner only while the partition epoch and ordered replica assignment equal the selection snapshot, the winner remains assigned, and the current leader remains unavailable. | The broker's [`UncleanRecoveryManager`](../crates/broker/src/unclean_recovery/manager.rs) post-poll commit path | [`proof.json`](../verif/krabka_verified_rlib/broker/unclean_recovery_commit_admission/proof.json) | The host captures the exact partition epoch and ordered replica IDs before polling, maps the re-read record and liveness result immediately before commit, and submits only after kernel admission. Metadata submission remains an asynchronous host boundary. |
| [`find_coordinator_admission`](../crates/verified/src/broker.rs) admits only known GROUP and TRANSACTION keys after their mapped Describe ACL succeeds, and well-formed SHARE keys at API v6+ after ClusterAction succeeds; malformed SHARE keys and unknown wire discriminants fail closed. | The broker's [`FindCoordinator` authorization adapter](../crates/broker/src/handlers/find_coordinator/authz.rs), before internal-topic creation or coordinator resolution | [`proof.json`](../verif/krabka_verified_rlib/broker/find_coordinator_admission/proof.json) | The host validates canonical SHARE composite keys and maps SHARE to `ClusterAction` on the singleton Cluster resource; only kernel allow outcomes enter resolution. |
| [`java_string_hash_partition`](../crates/verified/src/broker.rs) implements Java UTF-16 `String.hashCode`, Kafka's `Utils.abs(Integer.MIN_VALUE) = 0`, and positive partition modulo exactly. | The broker's [transaction coordinator partitioner](../crates/broker/src/txn/partitioner.rs) and [SHARE coordinator partitioner](../crates/broker/src/share_coordinator/partitioner.rs) | [`proof.json`](../verif/krabka_verified_rlib/broker/java_string_hash_partition/proof.json) | Both adapters pass `str::encode_utf16()` output, and configuration validation requires positive partition counts. The transaction adapter hashes the transactional ID exactly. The SHARE adapter first canonicalizes the composite key and topic UUID with Kafka's URL-safe unpadded encoding. The decision has no mutable state, so stale input does not apply; invalid counts fail closed, and exact retries are deterministic. |
| [`chain_step`](../crates/verified/src/chain.rs) accepts exactly the expected sequence and opaque previous head, advances by one, and rejects sequence exhaustion instead of wrapping. | [`krabka-audit`](../crates/audit/src/verify/walk.rs); the [`krabka-remote-storage`](../crates/remote-storage/src/worm/verify/walk.rs) WORM verifier | [`proof.json`](../verif/krabka_verified_rlib/chain/chain_step/proof.json) | The host computes `head_matches` by exact comparison with its independently recomputed chain head; hashing and signatures remain outside Creusot. |
| [`worm_signature_decision`](../crates/verified/src/worm.rs) admits only a present signature whose named key is externally trusted and whose canonical binding verifies; [`worm_object_set_decision`](../crates/verified/src/worm.rs) admits only a nonempty, unique, coordinate-matching, present, exact-count object set with matching sizes and, for deep verification, matching digests. | The WORM verifier's [signature adapter](../crates/remote-storage/src/worm/verify/signature.rs), [object adapter](../crates/remote-storage/src/worm/verify/objects.rs), and [chain walk](../crates/remote-storage/src/worm/verify/walk.rs) | [`signature proof`](../verif/krabka_verified_rlib/worm/worm_signature_decision/proof.json), [`object-set proof`](../verif/krabka_verified_rlib/worm/worm_object_set_decision/proof.json), and the shared [`chain-step proof`](../verif/krabka_verified_rlib/chain/chain_step/proof.json) | The host reads manifests through the fixed byte cap, rejects unknown JSON fields and format versions, recomputes the chain head from `canonical_manifest_bytes`, constructs the domain-separated signing bytes, compares the embedded public key with the externally trusted key, and delegates Ed25519 verification to `ring`. It parses every object filename and binds its base offset, segment UUID, and suffix to the signed segment identity; a one-time directory listing supplies exact same-segment object counts, presence, and sizes. Deep mode streams every current object version through SHA-256; shallow mode deliberately proves only set, coordinate, and size completeness and cannot detect a same-size substitution. Unsigned and untrusted manifests, orphan objects, duplicate keys, wrong coordinates, missing or extra objects, size mismatches, and deep digest mismatches make the programmatic report incomplete. The CLI may still grade unrelated orphan objects as a warning unless `--strict-orphans` is set. Listing consistency during the walk, object-store reads, canonical encoding, SHA-256, Ed25519, and key distribution remain host, crypto, or I/O assumptions. Tail truncation that removes both manifests and their objects requires an independently stored expected head. |
| [`select_chain_tip`](../crates/verified/src/chain.rs) selects the eligible WORM receipt with the greatest `(start offset, sequence)` rank; `chain_step` rejects writer-side sequence exhaustion instead of repeating `u64::MAX`. | The [`krabka-remote-storage`](../crates/remote-storage/src/worm/chain.rs) WORM writer and the broker [archive copy loop](../crates/broker/src/remote_log_manager/copy.rs) | [`proof.json`](../verif/krabka_verified_rlib/chain/select_chain_tip/proof.json) | The host marks a receipt eligible only after excluding delete states, decoding its metadata, and confirming that it carries a produced head; opaque heads remain outside Creusot. |
| [`compute_horizon`](../crates/verified/src/compaction.rs) adds retention time with saturation at the `i64` bounds. | `krabka-verified` calls it from `retain_decision`; [`krabka-log`](../crates/log/src/compact/decision.rs) also calls it in tests. | [`proof.json`](../verif/krabka_verified_rlib/compaction/compute_horizon/proof.json) | None. |
| [`retain_decision`](../crates/verified/src/compaction.rs) implements the KIP-534 record and control-marker retention rules. | [`krabka-log`](../crates/log/src/compact/decision.rs) | [`proof.json`](../verif/krabka_verified_rlib/compaction/retain_decision/proof.json) | None. |
| [`compaction_decode_step`](../crates/verified/src/compaction.rs) treats zero remaining input as the only clean end-of-stream and requires every successful batch decode to consume bytes. | [`krabka-log`](../crates/log/src/compact/batch_reader.rs), shared by compaction's map, transaction-survival, and rewrite passes | [`proof.json`](../verif/krabka_verified_rlib/compaction/compaction_decode_step/proof.json) | The host passes exact cursor lengths and the `RecordBatch::decode` result; corruption is surfaced before swap promotion. |
| [`retention_segment_evict`](../crates/verified/src/compaction.rs) admits time-based segment eviction only for a known timestamp whose host comparison has expired, while preserving independent size pressure. | `krabka-verified` calls it from its compaction tests. | [`proof.json`](../verif/krabka_verified_rlib/compaction/retention_segment_evict/proof.json) | None. |
| [`local_retention_prefix`](../crates/verified/src/retention.rs) selects only a contiguous oldest prefix required by age or size, stops at scheduled data, strictly reduces positive size debt when a removable segment has bytes, and reserves the final segment; [`retention_prefix`](../crates/verified/src/retention.rs) applies the corresponding finished-segment policy to tiered retention; [`retention_delete_target`](../crates/verified/src/retention.rs) advances an inclusive last offset without signed overflow. | The log's [local retention tick](../crates/log/src/log/tick.rs) and the broker's [local](../crates/broker/src/remote_log_manager/local_retention.rs) and [remote](../crates/broker/src/remote_log_manager/remote_retention.rs) KIP-405 retention walks | [`local prefix proof`](../verif/krabka_verified_rlib/retention/local_retention_prefix/proof.json), [`tiered prefix proof`](../verif/krabka_verified_rlib/retention/retention_prefix/proof.json), [`target proof`](../verif/krabka_verified_rlib/retention/retention_delete_target/proof.json) | The local host passes equal arrays in oldest-first sealed-segment order, exact segment sizes, the exact active-segment flag, an age-expired prefix from on-disk maximum timestamps, scheduled flags from a freshly scanned delivery watermark, and size debt computed from the configured `ByteSize` budget. It removes only the selected bases from memory and indexes, then attempts their required and optional files; file-delete errors leave recoverable orphan files but cannot expand the selected prefix. Tiered hosts map finished state and full-range timestamp comparisons exactly; remote candidates are already filtered and sorted. |
| [`election_jitter_ms`](../crates/verified/src/consensus.rs) keeps deterministic jitter in `[0, base_ms)`. | [`krabka-kraft-core`](../crates/kraft-core/src/core.rs) | [`proof.json`](../verif/krabka_verified_rlib/consensus/election_jitter_ms/proof.json) | None. |
| [`log_is_up_to_date`](../crates/verified/src/consensus.rs) implements the KIP-595 candidate-log ordering. | [`krabka-kraft-core`](../crates/kraft-core/src/core/vote_request.rs) | [`proof.json`](../verif/krabka_verified_rlib/consensus/log_is_up_to_date/proof.json) | None. |
| [`vote_wire_decision`](../crates/verified/src/vote.rs) rejects negative KIP-595 voter, candidate, and epoch fields; [`vote_encode_decision`](../crates/verified/src/vote.rs) rejects unsigned values above the signed wire maximum; [`vote_admission_decision`](../crates/verified/src/vote.rs) requires the exact target directory, matching cluster, and both voter memberships. | The raft [Vote codec](../crates/raft/src/kraft/transport/wire/request.rs) and kraft-core [Vote handler](../crates/kraft-core/src/core/vote_request.rs) | [`decode proof`](../verif/krabka_verified_rlib/vote/vote_wire_decision/proof.json), [`encode proof`](../verif/krabka_verified_rlib/vote/vote_encode_decision/proof.json), [`admission proof`](../verif/krabka_verified_rlib/vote/vote_admission_decision/proof.json) | The decoder also requires exactly one `__cluster_metadata` partition-0 row and full frame consumption. The host compares KIP-853 directory IDs exactly at kraft.version 1 and retains the version-0 node-ID compatibility rule; later epoch/log and double-vote checks remain in the state machine. |
| [`quorum_state_write_decision`](../crates/verified/src/quorum_state.rs) admits only schema v0 or v1 states whose term and optional leader, vote, and v0 voter identities fit their signed fields without clamping; [`quorum_state_load_decision`](../crates/verified/src/quorum_state.rs) restores only the exact no-vote sentinel or a nonnegative voted key from a valid version-specific layout. | KRaft's [quorum-state persistence adapter](../crates/raft/src/kraft/controller/quorum_state_file.rs) | [`write proof`](../verif/krabka_verified_rlib/quorum_state/quorum_state_write_decision/proof.json), [`load proof`](../verif/krabka_verified_rlib/quorum_state/quorum_state_load_decision/proof.json) | The host maps optional identities and exact numeric bounds into the write kernel. On load it supplies exact field-presence checks, configured-cluster equality, ordered v0 voter equality, and strict URL-safe 16-byte directory decoding. Checked Rust conversions and JSON, base64, and UUID codecs remain adapter assumptions. Leadership is volatile and is always cleared after admission. File create, write, file sync, and rename remain filesystem assumptions; failures are returned, retries overwrite the temporary file, malformed final files are ignored, and the parent directory is not synced. |
| [`voter_wire_decision`](../crates/verified/src/voter_set.rs) admits one `VotersRecord` member only with a nonnegative ID, byte-exact directory identity, a nonempty valid endpoint set, and a non-inverted nonnegative `kraft.version` range; [`voter_set_wire_decision`](../crates/verified/src/voter_set.rs) admits only record version 0 with a nonempty set of unique voter IDs. | KRaft's [`voter_set_from_wire`](../crates/raft/src/kraft/controller/control_state.rs) control-record adapter | [`voter proof`](../verif/krabka_verified_rlib/voter_set/voter_wire_decision/proof.json), [`set proof`](../verif/krabka_verified_rlib/voter_set/voter_set_wire_decision/proof.json) | The host preserves wire order, computes voter-ID uniqueness and per-voter listener-name uniqueness with ordered sets, and marks endpoints valid only when every name and host is nonempty and every port is nonzero. UUID conversion must reproduce all 16 input bytes; Kafka's zero UUID remains the exact legacy no-directory identity. Checked signed-to-unsigned conversions, string cloning, `VoterSet` construction, control-record decoding, and log replay remain adapter assumptions. The control history is mutated only after the complete record converts successfully, so a malformed record cannot partially install a voter set. |
| [`majority_size`](../crates/verified/src/consensus.rs) computes `floor(voters / 2) + 1`; [`election_has_quorum`](../crates/verified/src/consensus.rs) admits only that many unique grants from the current voter set. | [`krabka-kraft-core`](../crates/kraft-core/src/core/election.rs) | [`majority_size/proof.json`](../verif/krabka_verified_rlib/consensus/majority_size/proof.json), [`election_has_quorum/proof.json`](../verif/krabka_verified_rlib/consensus/election_has_quorum/proof.json) | The host filters the grant set to unique current voters before passing its count. |
| [`failover_action`](../crates/verified/src/consensus.rs) gives clean election precedence, prevents witnesses from becoming leaders, selects configured offset-aware recovery before unclean election, and otherwise shrinks or leaves the ISR unchanged. | The broker's shared [leader-failover policy](../crates/broker/src/leader_election/policy.rs) | [`proof.json`](../verif/krabka_verified_rlib/consensus/failover_action/proof.json) | The host filters live ISR members, classifies electable non-witness candidates, maps the resolved recovery strategy exactly, and constructs the selected ISR after the kernel chooses the action class. |
| [`feature_update_decision`](../crates/verified/src/features.rs) rejects a feature finalization unless its wire update type, supported range, direction, registered-node support, representability floor, dependencies, and delete semantics are valid; lossy cleanup is admitted only for an unsafe downgrade and selects cleanup-before-feature emission. | The `UpdateFeatures` [per-feature validator](../crates/broker/src/handlers/update_features/validate.rs) | [`proof.json`](../verif/krabka_verified_rlib/features/feature_update_decision/proof.json) | The host decodes request-version-specific upgrade types, folds every registered broker and controller range, rejects an unregistered quorum controller during online metadata downgrades, derives cleanup records from the same immutable metadata image, projects those records before checking the target floor and dependencies, and defers the metadata-version record until after all cleanup records. Feature-registry definitions, metadata-image invariants, cleanup-record construction, controller submission, durability, and races after the captured image remain host assumptions; the request protocol has no image epoch for stale-client fencing. |
| [`select_best_recovery_replica`](../crates/verified/src/consensus.rs) returns an in-bounds replica whose last-written leader epoch is maximal, whose log end is maximal within that epoch, and whose broker ID is minimal on a complete tie. | The broker's KIP-966 [unclean-recovery selector](../crates/broker/src/unclean_recovery/selection.rs) | [`proof.json`](../verif/krabka_verified_rlib/consensus/select_best_recovery_replica/proof.json) | The host preserves response order while adapting each report to `(last written leader epoch, log end offset, broker ID)` and maps the proved index back to that same report. |
| [`reassignment_action`](../crates/verified/src/reassignment.rs) waits until every addition is caught up, completes immediately when the leader remains assigned, and otherwise selects the first eligible replacement leader or waits. | The broker's [partition-reassignment policy](../crates/broker/src/reassignment/policy.rs) | [`proof.json`](../verif/krabka_verified_rlib/reassignment/reassignment_action/proof.json) | The host constructs the target assignment as current replicas minus removals and marks a handoff candidate eligible exactly when it is both in the ISR and alive. |
| [`reassignment_set_membership`](../crates/verified/src/reassignment.rs) proves the pointwise union, addition, and removal classification for a start plan; [`reassignment_plan_admission`](../crates/verified/src/reassignment.rs) admits a start or cancel mutation only after its target/RF or approval/in-progress fences, leader eligibility, and checked epoch are established. | The `AlterPartitionReassignments` [per-row planner](../crates/broker/src/handlers/alter_partition_reassignments/plan.rs), before constructing a controller record | [`set proof`](../verif/krabka_verified_rlib/reassignment/reassignment_set_membership/proof.json), [`admission proof`](../verif/krabka_verified_rlib/reassignment/reassignment_plan_admission/proof.json) | The host rejects empty, duplicate, negative, and unregistered requested targets; enforces the RF policy against the current target; walks each unique current-or-target replica through the set kernel in stable current-then-target order; and selects a leader from the planned replicas intersected with the ISR. The cancel approval is resolved against the same metadata image used for planning. Metadata-image invariants, broker registration changes, break-glass registry correctness, directory remapping, asynchronous controller submission, and races after the snapshot remain host assumptions. The shared checked-epoch adapter fails closed before admission. |
| [`broker_registration_decision`](../crates/verified/src/registration.rs) admits only a compatible new registration, returns the exact stored epoch for an idempotent retry of the same incarnation, and rejects a competing incarnation; [`broker_heartbeat_decision`](../crates/verified/src/registration.rs) requires exact broker-epoch equality and derives caught-up, fail-closed fencing, and shutdown flags from that registration. | The controller listener's [broker-registration](../crates/raft/src/server/registration/broker.rs) handler and the broker's [heartbeat validation](../crates/broker/src/handlers/broker_heartbeat/validation.rs) | [`registration proof`](../verif/krabka_verified_rlib/registration/broker_registration_decision/proof.json), [`heartbeat proof`](../verif/krabka_verified_rlib/registration/broker_heartbeat_decision/proof.json) | Before invoking the registration action, the host parses a nonnegative broker ID, matches either canonical cluster-ID encoding, rejects migrating brokers, validates a nonempty uniquely named listener set, and folds every finalized feature against the advertised ranges from one image. The controller stamps a new broker epoch from the committed metadata-log offset. Heartbeat validation reads the stored epoch from one image and passes the proved catch-up, fence, and shutdown decisions into the controller's stateful liveness and drain adapters. Request decoding, authorization, leader routing, metadata validation, liveness timing, log durability, and leadership or image changes after the snapshot remain host and I/O assumptions. |
| [`recompute_high_watermark`](../crates/verified/src/consensus.rs) computes the majority offset, applies the epoch gate, and does not regress. | [`krabka-kraft-core`](../crates/kraft-core/src/core/replication.rs); the `krabka-broker` WAL [live engine](../crates/broker/src/wal/quorum/engine/distributed.rs); the [diskless crash model](../crates/broker/src/diskless_crash_model.rs) | [`proof.json`](../verif/krabka_verified_rlib/consensus/recompute_high_watermark/proof.json) | `1 <= majority <= followers + 1`; `current_hwm <= log_end`; each follower offset is at most `log_end`. |
| [`break_glass_admission`](../crates/verified/src/break_glass.rs) applies the fail-closed withdrawn, consumed, expiry, approval, and signature checks in order; [`select_break_glass_candidate`](../crates/verified/src/break_glass.rs) deterministically chooses the earliest expiry and then lowest UUID. | The broker's break-glass [usability gate](../crates/broker/src/break_glass/gate/usability.rs) and [proposal selector](../crates/broker/src/break_glass/gate.rs) | [`admission proof`](../verif/krabka_verified_rlib/break_glass/break_glass_admission/proof.json), [`selection proof`](../verif/krabka_verified_rlib/break_glass/select_break_glass_candidate/proof.json) | The host supplies exact distinct-approval and signature facts and maps UUIDs to their canonical high and low halves. Atomic consumption and action persistence remain outside Creusot. |
| [`directory_assignment_decision`](../crates/verified/src/directory.rs) emits a change only for the reporting broker's exact replica slot and makes an already-current assignment a no-op; [`directory_response_decision`](../crates/verified/src/directory.rs) accepts controller success only while the responding controller remains the current leader. | The `AssignReplicasToDirs` [request-to-delta planner](../crates/broker/src/handlers/assign_replicas_to_dirs/changes.rs) and broker [directory-report sender](../crates/broker/src/assign_dirs.rs) | [`planning proof`](../verif/krabka_verified_rlib/directory/directory_assignment_decision/proof.json), [`response proof`](../verif/krabka_verified_rlib/directory/directory_response_decision/proof.json) | The host resolves the topic UUID and partition from one metadata image, derives the unique reporting-replica position from the ordered replica vector, and emits the replica ID read back from that proved position. Applying the narrow `PartitionDirAssignment` delta preserves replica order and all other slots; metadata-image uniqueness and delta-apply semantics remain host assumptions. The sender compares the captured and current controller identities after decoding the response; transport integrity and a leader change after that final check remain outside the proof. |
| [`diskless_trim_decision`](../crates/verified/src/diskless.rs) advances local log start only behind both the committed object projection and the lagged high watermark, without subtraction overflow or regression. | The diskless WAL [object flusher](../crates/broker/src/diskless/flusher/object_flush.rs) | [`proof.json`](../verif/krabka_verified_rlib/diskless/diskless_trim_decision/proof.json) | None; negative offsets and a safety lag beyond the high watermark fail closed, while a negative configured lag preserves the host's existing zero-lag behavior. |
| [`diskless_logical_range`](../crates/verified/src/diskless.rs) selects the indexed range covering a requested offset or the immediate successor after a gap; [`diskless_span_extension`](../crates/verified/src/diskless.rs) extends only within one object across contiguous whole ranges and under the response cap; [`diskless_batch_step`](../crates/verified/src/diskless.rs) advances decoded batches without splitting them or overflowing logical or byte coordinates. | The diskless WAL cold-read [index lookup](../crates/broker/src/diskless/wal_index.rs) and [batch selector](../crates/broker/src/diskless/read.rs) | [`logical range proof`](../verif/krabka_verified_rlib/diskless/diskless_logical_range/proof.json), [`span proof`](../verif/krabka_verified_rlib/diskless/diskless_span_extension/proof.json), [`batch proof`](../verif/krabka_verified_rlib/diskless/diskless_batch_step/proof.json) | The host copies ranges in `BTreeMap` order and rejects invalid, overlapping, or unordered logical ranges before calling the selection kernel. Object-key equality is exact. Object reads, index completeness, decoded `RecordBatch` framing and CRC, and actual decode consumption remain host and I/O assumptions. Kafka permits the first returned batch to exceed the cap; every later batch must fit without splitting. |
| [`exact_epoch_successor`](../crates/verified/src/epoch.rs) returns the exact signed successor only below the caller's maximum and rejects the maximum or any value above it. | The broker's shared [metadata-epoch adapter](../crates/broker/src/metadata_epoch.rs), used by ISR updates, leader election and failover, reassignment, unclean recovery, classic and next-generation group coordination, barrier injection, share-state resets, and share snapshots | [`proof.json`](../verif/krabka_verified_rlib/epoch/exact_epoch_successor/proof.json) | None. The host passes `i32::MAX` for Kafka `int32` metadata fields and `i64::MAX` for barrier and snapshot epochs, then rejects `None` before mutation or persistence. Fetch-session and share-fetch-session epochs are excluded because their protocols intentionally wrap while skipping reserved sentinels. |
| [`barrier_cut_expired`](../crates/verified/src/retention.rs) expires a held barrier cut only when a positive retained-cut count yields a representable cutoff and the held epoch is at or below that exact cutoff. | The barrier coordinator's [retention selector](../crates/broker/src/barrier/state/retention.rs); barrier epoch allocation uses the separately proved `exact_epoch_successor` adapter above | [`proof.json`](../verif/krabka_verified_rlib/retention/barrier_cut_expired/proof.json) | Barrier retention is count-based, as persisted in `GroupValue.retained_cuts`; no timestamp, duration, or wall clock participates. The host supplies the newly published epoch, persisted count, and exact held epochs. Persistence appends tombstones for precisely the returned epochs, and recovery may replay the prior state until those records commit. |
| [`create_token_deadlines`](../crates/verified/src/delegation_token.rs), [`renew_token_expiry`](../crates/verified/src/delegation_token.rs), [`expire_token_deadline`](../crates/verified/src/delegation_token.rs), and [`token_is_active`](../crates/verified/src/delegation_token.rs) implement KIP-48 deadline selection and authentication admission without signed overflow or resurrection of expired tokens. | The broker's [create](../crates/broker/src/handlers/create_delegation_token/lifetime.rs), [renew](../crates/broker/src/handlers/renew_delegation_token.rs), [expire](../crates/broker/src/handlers/expire_delegation_token.rs), and [SCRAM](../crates/broker/src/network/auth/scram.rs) paths | [`create proof`](../verif/krabka_verified_rlib/delegation_token/create_token_deadlines/proof.json), [`renew proof`](../verif/krabka_verified_rlib/delegation_token/renew_token_expiry/proof.json), [`expire proof`](../verif/krabka_verified_rlib/delegation_token/expire_token_deadline/proof.json), [`authentication proof`](../verif/krabka_verified_rlib/delegation_token/token_is_active/proof.json) | Owner/renewer authorization and controller persistence remain in the host handlers; renew/expire still depend on an image snapshot followed by an unconditional controller write. |
| [`token_api_admission`](../crates/verified/src/delegation_token.rs) requires a non-anonymous authenticated identity for every token API and permits token-authenticated sessions to describe, but never create, renew, or expire, tokens. | The broker's shared [`ConnectionAuth` adapter](../crates/broker/src/network/auth/state.rs), called by all four delegation-token handlers before token access or mutation | [`proof.json`](../verif/krabka_verified_rlib/delegation_token/token_api_admission/proof.json) | The host maps only `AuthMethod::Anonymous` and unestablished connection states to an absent authenticated identity. |
| [`token_describe_visible`](../crates/verified/src/delegation_token.rs) restricts token-authenticated callers to tokens they own and otherwise requires the owner filter plus an owner, renewer, or Describe-ACL relationship. | The broker's [`DescribeDelegationToken` filter](../crates/broker/src/handlers/describe_delegation_token.rs) | [`proof.json`](../verif/krabka_verified_rlib/delegation_token/token_describe_visible/proof.json) | The host compares canonical principals, treats a missing or empty wire owner list as no filter, and passes the authorizer's exact `TOKEN:<owner>` Describe result. |
| [`scram_credential_source`](../crates/verified/src/delegation_token.rs) always prefers a regular SCRAM credential, permits delegation-token fallback only for SCRAM-SHA-256, distinguishes expired tokens, and fails unknown users closed. | The broker's [SCRAM round-one credential lookup](../crates/broker/src/network/auth/scram.rs) | [`proof.json`](../verif/krabka_verified_rlib/delegation_token/scram_credential_source/proof.json) | The host performs exact metadata lookups and derives token activity through the separately proved `token_is_active` kernel before synthesizing a KIP-48 credential. |
| [`handoff_high_watermark`](../crates/verified/src/consensus.rs) returns the maximum of the old and new frontiers. | No host caller. | [`proof.json`](../verif/krabka_verified_rlib/consensus/handoff_high_watermark/proof.json) | None. |
| [`epoch_and_offset_for_entries`](../crates/verified/src/leader_epoch.rs) returns the KIP-101 leader epoch and offset where a follower truncates its divergent suffix. | [`krabka-log`](../crates/log/src/leader_epoch_checkpoint/lookup.rs) | [`proof.json`](../verif/krabka_verified_rlib/leader_epoch/epoch_and_offset_for_entries/proof.json) | Entries have strictly increasing leader epochs and start offsets. |
| [`offset_index_lookup`](../crates/verified/src/log_index.rs) returns the position for the greatest relative offset at or below the target. | [`krabka-log`](../crates/log/src/index/offset.rs) | [`proof.json`](../verif/krabka_verified_rlib/log_index/offset_index_lookup/proof.json) | Entries have strictly increasing relative offsets. |
| [`offset_index_position_at_or_after`](../crates/verified/src/log_index.rs) returns the position for the least relative offset at or above the target. | [`krabka-log`](../crates/log/src/index/offset.rs) | [`proof.json`](../verif/krabka_verified_rlib/log_index/offset_index_position_at_or_after/proof.json) | Entries have strictly increasing relative offsets. |
| [`time_index_lookup`](../crates/verified/src/log_index.rs) returns the relative offset for the last timestamp at or below the target. | [`krabka-log`](../crates/log/src/index/time.rs) | [`proof.json`](../verif/krabka_verified_rlib/log_index/time_index_lookup/proof.json) | Entries have nondecreasing timestamps. |
| [`first_timestamp_index`](../crates/verified/src/timestamp.rs) selects the first decoded record at or after a target; [`earliest_max_timestamp_index`](../crates/verified/src/timestamp.rs) selects the earliest maximum timestamp; [`timestamp_record_coordinates`](../crates/verified/src/timestamp.rs) derives absolute record coordinates without signed overflow; [`timestamp_scan_next`](../crates/verified/src/timestamp.rs) advances strictly past a fresh decoded batch; and [`timestamp_scan_window`](../crates/verified/src/timestamp.rs) doubles only a positive representable retry window. | The local segment [timestamp scan](../crates/log/src/segment/timestamp_scan.rs), for indexed lookup and maximum-timestamp recovery | [`first-match proof`](../verif/krabka_verified_rlib/timestamp/first_timestamp_index/proof.json), [`earliest-maximum proof`](../verif/krabka_verified_rlib/timestamp/earliest_max_timestamp_index/proof.json), [`coordinate proof`](../verif/krabka_verified_rlib/timestamp/timestamp_record_coordinates/proof.json), [`cursor proof`](../verif/krabka_verified_rlib/timestamp/timestamp_scan_next/proof.json), [`window proof`](../verif/krabka_verified_rlib/timestamp/timestamp_scan_window/proof.json) | The host supplies CRC-validated batches and records in on-disk order. The time-index floor does not skip the earliest qualifying record, and a segment's stored maximum timestamp equals the maximum decoded record timestamp, so the first record at or above it is an exact earliest maximum. Each read returns whole batches in order. Decode and file I/O remain host assumptions; read, coordinate, stale-cursor, and retry-window failures return no result. |
| [`stamp_ranges_valid`](../crates/verified/src/stamp.rs) accepts exactly sorted, nonoverlapping, non-inverted inclusive ranges; [`stamp_range_insertion_index`](../crates/verified/src/stamp.rs) finds a boundary-preserving insertion point or rejects overlap; [`exact_stamp_range_index`](../crates/verified/src/stamp.rs) selects only an exact pair of range boundaries for idempotent replacement or removal; and [`covering_stamp_range_index`](../crates/verified/src/stamp.rs) is equivalent to inclusive coverage. | The local log's [commit-stamp sidecar](../crates/log/src/stamp_index.rs), across open, append, transactional upsert, startup removal, and point lookup | [`validation proof`](../verif/krabka_verified_rlib/stamp/stamp_ranges_valid/proof.json), [`insertion proof`](../verif/krabka_verified_rlib/stamp/stamp_range_insertion_index/proof.json), [`exact-range proof`](../verif/krabka_verified_rlib/stamp/exact_stamp_range_index/proof.json), [`coverage proof`](../verif/krabka_verified_rlib/stamp/covering_stamp_range_index/proof.json) | The host decodes complete fixed-width big-endian entries, sorts them by both boundaries and stamp, removes only fully identical retry records, and validates the result before installation. Successful mutations keep the in-memory ranges in canonical order; exact upsert and removal rewrite the sidecar before swapping memory. File reads, appends, rewrites, and synchronization remain I/O assumptions. A partial fixed-width record or conflicting persisted range fails startup closed; an interrupted in-place rewrite may require log recovery, while an uncertain append may leave an exact duplicate that startup safely canonicalizes. |
| [`isr_high_watermark`](../crates/verified/src/isr.rs) computes the minimum log end represented by a classic Kafka leader and its ISR followers. | [`krabka-broker`](../crates/broker/src/replica_state.rs) | [`proof.json`](../verif/krabka_verified_rlib/isr/isr_high_watermark/proof.json) | The host identifies the current leader, includes every other ISR member, and substitutes zero for missing follower progress during the leadership-change reseed window. |
| [`isr_maintenance_selected`](../crates/verified/src/isr.rs) retains an assigned leader, retains assigned ISR followers only while fetch-recent, and admits assigned non-ISR replicas only when both fetch-recent and recently caught up; [`isr_proposal_changed`](../crates/verified/src/isr.rs) suppresses exactly proposals with no unique additions or removals. | The broker's ISR maintenance [proposal builder](../crates/broker/src/isr_maintenance/proposal.rs) | [`selection proof`](../verif/krabka_verified_rlib/isr/isr_maintenance_selected/proof.json), [`no-op proof`](../verif/krabka_verified_rlib/isr/isr_proposal_changed/proof.json) | The replica-state installation retains the exact metadata assignment and leader. The host makes one decision per sorted `BTreeSet` candidate, so output is unique and sorted. `Instant` is monotonic; `last_fetch` records liveness, and `last_caught_up` is refreshed only by a fetch whose follower LEO reached the observed leader LEO. The scan's single timestamp and configured lag bound supply both recency facts. |
| [`isr_admission`](../crates/verified/src/isr.rs) applies AlterPartition's ordered fail-closed checks: leader-epoch fencing, non-empty valid replica subset, broker-epoch eligibility, then admission. | The broker's [`AlterPartition` row adapter](../crates/broker/src/handlers/alter_partition/isr_update.rs) | [`proof.json`](../verif/krabka_verified_rlib/isr/isr_admission/proof.json) | The host rejects negative wire broker IDs, checks every proposed member against the partition replicas, and validates every supplied non-sentinel broker epoch against the current registration. |
| [`wal_reservation_epoch_ready`](../crates/verified/src/offset_allocator.rs) requires the current leader marker to be committed before allocation; [`wal_reservation_frontier`](../crates/verified/src/offset_allocator.rs) extends only an exact positive, non-overflowing chain of uncommitted reservations; [`reserve_offsets`](../crates/verified/src/offset_allocator.rs) returns the exact durable base and next offset; and [`wal_reservation_response`](../crates/verified/src/offset_allocator.rs) admits only a matching request, valid range, and current leader epoch. | The KRaft controller's [reservation submit path](../crates/raft/src/kraft/controller/submit.rs) and the broker's [WAL offset sequencer](../crates/broker/src/wal/offset_sequencer.rs) | [`epoch-readiness proof`](../verif/krabka_verified_rlib/offset_allocator/wal_reservation_epoch_ready/proof.json), [`pending-frontier proof`](../verif/krabka_verified_rlib/offset_allocator/wal_reservation_frontier/proof.json), [`allocation proof`](../verif/krabka_verified_rlib/offset_allocator/reserve_offsets/proof.json), and [`response proof`](../verif/krabka_verified_rlib/offset_allocator/wal_reservation_response/proof.json) | The controller actor serializes submissions and preserves append order in its waiter list. A new leader rejects reservations until its epoch marker crosses the HWM, so every inherited entry before that marker is committed and applied or has been truncated; within one ready epoch, the host folds every same-partition waiter in append order before allocating. Counts are positive, coordinates are nonnegative, and arithmetic must fit `i64`. Results are released only after their metadata batch commits and applies; leadership loss fails the old waiter, and a retry waits for the next epoch marker before allocation. The broker binds the single response row to the exact topic, partition, count, and locally observed epoch. A broker-only observer cannot read a controller epoch, so it requires a nonnegative response epoch and relies on the private one-shot RPC to correlate the committed response. Log durability, HWM correctness, metadata encoding and replay, RPC correlation, transport, and I/O remain host assumptions. Malformed, stale, zero-count, overflowing, unready-epoch, and failed responses fail closed. |
| [`producer_decision`](../crates/verified/src/producer.rs) classifies an idempotent-producer batch as append, duplicate, out of order, or fenced. | [`krabka-broker`](../crates/broker/src/producer_state/decision.rs); [`increment_sequence`](../crates/verified/src/producer.rs) is also called by [`krabka-log`](../crates/log/src/log/open.rs). | [`proof.json`](../verif/krabka_verified_rlib/producer/producer_decision/proof.json) | None. |
| [`producer_snapshot_latest_index`](../crates/verified/src/producer_snapshot.rs) selects the greatest nonnegative snapshot offset at or below the captured log end without relying on directory order; [`producer_snapshot_entry_valid`](../crates/verified/src/producer_snapshot.rs) admits only exact marker sentinels or producer state strictly before the snapshot boundary; [`producer_snapshot_replay_start`](../crates/verified/src/producer_snapshot.rs) starts at the greater of the snapshot and local-log starts; [`producer_snapshot_retained`](../crates/verified/src/producer_snapshot.rs) keeps exactly snapshots at or below a truncation cut. | The log's [snapshot codec and selector](../crates/log/src/producer_snapshot.rs), [open-time recovery](../crates/log/src/log/open.rs), and [tail truncation](../crates/log/src/log/truncate.rs); recovered batches also use the bounded [`replay_batch_cursor_decision`](../crates/verified/src/recovery.rs). | [`selection proof`](../verif/krabka_verified_rlib/producer_snapshot/producer_snapshot_latest_index/proof.json), [`entry proof`](../verif/krabka_verified_rlib/producer_snapshot/producer_snapshot_entry_valid/proof.json), [`replay-start proof`](../verif/krabka_verified_rlib/producer_snapshot/producer_snapshot_replay_start/proof.json), [`retention proof`](../verif/krabka_verified_rlib/producer_snapshot/producer_snapshot_retained/proof.json), and [`batch-cursor proof`](../verif/krabka_verified_rlib/recovery/replay_batch_cursor_decision/proof.json) | The host supplies offsets parsed from canonical fixed-width filenames and every decoded entry field unchanged. CRC, byte decoding, directory enumeration, and `HashMap` duplicate detection remain host assumptions; malformed or future-state snapshots are removed and recovery retries the next proved candidate. The captured log end bounds every replay batch, and the admitted exclusive cursor is threaded into the next read. Snapshot create, file sync, rename, deletion, and crash atomicity remain filesystem assumptions; the parent directory is not synced. |
| [`local_recovery_segment_chain`](../crates/verified/src/local_recovery.rs) accepts only nonnegative strictly ordered segment bases; [`local_recovery_sealed_last`](../crates/verified/src/local_recovery.rs) closes each sealed range exactly before the next base; [`local_recovery_batch_step`](../crates/verified/src/local_recovery.rs) advances one maximal valid byte-and-offset prefix without overlap or overflow; [`local_recovery_index_frontier`](../crates/verified/src/local_recovery.rs) derives the exact representable exclusive index frontier; and [`local_recovery_swap_action`](../crates/verified/src/local_recovery.rs) distinguishes an authoritative original, a full swap promotion, continuation after the log rename, and sidecars with no log. | The log's [swap-orphan recovery](../crates/log/src/recovery.rs), [segment-chain open](../crates/log/src/log/open.rs), and active-segment [torn-tail scan and sparse-index rebuild](../crates/log/src/segment/open.rs) | [`chain proof`](../verif/krabka_verified_rlib/local_recovery/local_recovery_segment_chain/proof.json), [`sealed-boundary proof`](../verif/krabka_verified_rlib/local_recovery/local_recovery_sealed_last/proof.json), [`batch-step proof`](../verif/krabka_verified_rlib/local_recovery/local_recovery_batch_step/proof.json), [`index-frontier proof`](../verif/krabka_verified_rlib/local_recovery/local_recovery_index_frontier/proof.json), and [`swap-action proof`](../verif/krabka_verified_rlib/local_recovery/local_recovery_swap_action/proof.json) | The host parses canonical fixed-width segment and swap names, sorts and deduplicates their bases, and passes decoded batch bases, deltas, and exact consumed byte lengths unchanged. With open-time validation enabled, it scans the active segment from byte zero, stops at the first framing, CRC, overlap, or arithmetic failure, truncates only after every byte and offset frontier is representable, and rebuilds both sparse indexes from that same retained prefix at the configured interval. Disabling open-time validation opts out of this guarantee. Directory enumeration, record decoding, file truncation, index rewrite, rename, deletion, synchronization, concurrent directory stability, and crash atomicity remain host or filesystem assumptions; a retry continues sidecar promotion after a completed log rename, while sidecars with neither a final nor swap log fail closed. |
| [`produce_batch_admission`](../crates/verified/src/produce.rs) accepts exactly a nonempty data batch with base offset zero, consistent positive record count, a valid producer sequence when idempotent, and CreateTime timestamps; malformed headers take precedence over timestamp rejection. | The broker's [Produce batch preparation](../crates/broker/src/handlers/produce/prepare.rs) | [`proof.json`](../verif/krabka_verified_rlib/produce/produce_batch_admission/proof.json) | CRC validation runs first; the host maps the decoded control and timestamp-type attributes exactly. |
| [`producer_id_block_allocation`](../crates/verified/src/producer_id.rs) admits only a registered broker with the exact nonnegative epoch and current nonnegative frontier, then constructs one positive contiguous block whose durable successor is exactly `first + len` without signed overflow. | The broker's shared [producer-ID allocator](../crates/broker/src/producer_id_manager.rs), called by [`AllocateProducerIds`](../crates/broker/src/handlers/allocate_producer_ids.rs) and local producer identity allocation | [`decision proof`](../verif/krabka_verified_rlib/producer_id/producer_id_block_allocation/proof.json), [`decision clone proof`](../verif/krabka_verified_rlib/producer_id/impl_Clone_for_ProducerIdBlockAllocationDecision/clone/proof.json), and [`plan clone proof`](../verif/krabka_verified_rlib/producer_id/impl_Clone_for_ProducerIdBlockPlan/clone/proof.json) | The host maps the request broker ID and epoch, the current registered epoch, and the committed `next_producer_id` exactly. This adapter is the broker's only production producer of `ProducerIdsRecord` and submits the proved `plan.next`; success is returned only after the controller accepts that record. The controller serializes competing records, rechecks the broker epoch and a strictly advancing frontier, and the loser retries from the refreshed committed image. Raft durability, image freshness after a rejected concurrent proposal, controller failover, and response delivery remain host or I/O assumptions. An uncertain success can leave an unused block, but no block is reused or overlapped. |
| [`produce_durability_frontier`](../crates/verified/src/produce.rs) returns exactly `base offset + last offset delta + 1`, rejects negative inputs, and fails closed on signed overflow. | The broker's shared [Produce durability adapter](../crates/broker/src/handlers/produce.rs), used by the fresh-append [acknowledgement path](../crates/broker/src/handlers/produce/append.rs) and idempotent [duplicate path](../crates/broker/src/handlers/produce/producer_checks.rs) | [`proof.json`](../verif/krabka_verified_rlib/produce/produce_durability_frontier/proof.json) | Batch admission establishes a nonnegative delta, the writer supplies the assigned nonnegative base, and producer-state duplicates preserve that same base and span. An invalid or overflowing frontier returns `INVALID_RECORD` without starting a durability wait or recording a dedup commit. High-watermark publication, waiter notification, deadlines, leadership changes, and producer-state concurrency remain asynchronous host assumptions. |
| [`advance_high_watermark`](../crates/verified/src/raft.rs) advances a KRaft HWM monotonically without passing log end; [`in_half_open_window`](../crates/verified/src/raft.rs) and [`frontier_reaches`](../crates/verified/src/raft.rs) define commit, fetch, apply, and waiter boundaries. | [`krabka-raft`](../crates/raft/src/kraft/log.rs) and its controller [offset rules](../crates/raft/src/kraft/controller/offsets.rs) | [`advance_high_watermark/proof.json`](../verif/krabka_verified_rlib/raft/advance_high_watermark/proof.json), [`in_half_open_window/proof.json`](../verif/krabka_verified_rlib/raft/in_half_open_window/proof.json), [`frontier_reaches/proof.json`](../verif/krabka_verified_rlib/raft/frontier_reaches/proof.json) | The HWM bound requires the host invariant `previous <= log_end`; the predicates accept all integers. |
| [`control_history_frontier`](../crates/verified/src/raft.rs) returns the exact prefix of strictly ordered control-record offsets below a half-open frontier, so the prefix excludes a record exactly at the frontier and its suffix contains every record at or above it. | The KRaft controller's voter and feature [control history](../crates/raft/src/kraft/controller/control_state.rs), for point lookup, truncation, and commitment | [`proof.json`](../verif/krabka_verified_rlib/raft/control_history_frontier/proof.json) | The host copies keys from a `BTreeMap`, whose unique ascending iteration establishes the kernel's strict-order precondition, and maps the proved last prefix key back into that same map. Truncation splits at the proved first suffix key. Commitment is called with the controller's monotonic HWM and selects only the latest record strictly below it. |
| [`metadata_record_coordinates`](../crates/verified/src/raft.rs) assigns each nonempty metadata batch the exact contiguous record deltas `0..n-1`, rejects indexes outside the batch, and rejects counts whose final delta does not fit Kafka's signed field. | The KRaft controller's [`metadata_record_batch`](../crates/raft/src/kraft/controller/records.rs) builder | [`proof.json`](../verif/krabka_verified_rlib/raft/metadata_record_coordinates/proof.json) | The host passes the exact slice length and each `enumerate` index; an empty slice produces an empty batch with delta zero, and any coordinate rejection occurs before append. |
| [`fetch_response_mutation`](../crates/verified/src/raft.rs) rejects a KRaft Fetch response unless its sender, advertised leader, current leader, live fetching role, and epoch all agree, then selects exactly one of snapshot, truncation, append, or HWM advancement. | The KRaft controller's [`on_fetch_response`](../crates/raft/src/kraft/controller/replication.rs) adapter, immediately after wire decode and before peer discovery or response-derived mutation | [`proof.json`](../verif/krabka_verified_rlib/raft/fetch_response_mutation/proof.json) | The host maps `Follower` and an `Observer` with a known leader to the role-leader input, maps every other role to `None`, and passes the decoded sender, leader, epoch, and mutation-presence flags unchanged. Wire decoding itself is outside Creusot. |
| [`voter_reconfiguration_decision`](../crates/verified/src/reconfiguration.rs) admits one KIP-853 add, remove, update, or `kraft.version` finalization only for a current-epoch leader with no active change; it preserves a nonempty voter set and constructs the exact next count, version, and control-record shape. | The KRaft controller's [`on_reconfigure`](../crates/raft/src/kraft/controller/reconfiguration.rs) adapter before any voter-state mutation or control-record append | [`proof.json`](../verif/krabka_verified_rlib/reconfiguration/voter_reconfiguration_decision/proof.json) | The host takes membership and `kraft.version` from one committed control-state snapshot, requires the latest state to equal it and no pending waiter, and maps epoch commitment from `HWM > epoch_start_offset`. It compares removal directory IDs exactly; update also accepts the legacy nil directory only during version-0 preflight. Version ranges come unchanged from decoded voter metadata. Observer catch-up uses the latest recorded fetch offset against the current log end; that liveness observation can become stale after admission, while safety still depends on the current quorum committing the appended voter record. The host checks the proved next count against the constructed set. |
| [`replay_record_decision`](../crates/verified/src/recovery.rs) applies only records with overflow-safe offsets inside `[from, high watermark)` and separates control from data replay; [`replay_cursor_decision`](../crates/verified/src/recovery.rs) advances only on strict progress; [`replay_batch_cursor_decision`](../crates/verified/src/recovery.rs) accepts a decoded batch only when its base does not overlap the cursor and its exclusive successor is representable, strictly advancing, and at or below the captured end; [`should_capture_first_downgrade`](../crates/verified/src/recovery.rs) retains only the first downgrade. | The KRaft controller's [committed data, control-state, and control-record recovery walks](../crates/raft/src/kraft/controller/recovery.rs), the group coordinator's [`__consumer_offsets` replay](../crates/broker/src/coordinator/bootstrap/replay.rs), and the barrier coordinator's [`__barrier_state` replay](../crates/broker/src/barrier/coordinator/recovery.rs) | [`record proof`](../verif/krabka_verified_rlib/recovery/replay_record_decision/proof.json), [`cursor proof`](../verif/krabka_verified_rlib/recovery/replay_cursor_decision/proof.json), [`batch cursor proof`](../verif/krabka_verified_rlib/recovery/replay_batch_cursor_decision/proof.json), [`downgrade proof`](../verif/krabka_verified_rlib/recovery/should_capture_first_downgrade/proof.json) | Batch framing, CRC checking, and record/control decoding remain in the host. Each caller passes the decoded batch base and signed last delta unchanged, threads the admitted exclusive end into the next call, and captures the log end before replay. Kafka compaction may leave forward gaps, which the kernel permits. The group coordinator returns a startup error for a rejected batch; barrier recovery warns and stops that partition before applying the rejected batch. |
| [`remote_metadata_partition`](../crates/verified/src/remote_metadata.rs) implements Kafka's KIP-405 metadata partitioner exactly: Java UUID-half hashing, big-endian `int` serialization, Murmur2, positive masking, and modulo; nonpositive partition counts are rejected. | [`krabka-remote-storage-topic`](../crates/remote-storage-topic/src/partitioning.rs) | [`proof.json`](../verif/krabka_verified_rlib/remote_metadata/remote_metadata_partition/proof.json) | `Uuid::as_u128` preserves the canonical most-significant and least-significant 64-bit images consumed by the kernel. |
| [`remote_metadata_resume_cursor`](../crates/verified/src/remote_metadata.rs) maps the sole no-event sentinel `-1` to zero, advances every real committed offset by exactly one, and rejects offsets below the sentinel or at `i64::MAX`. | The topic-backed remote-metadata manager's [snapshot adapter](../crates/remote-storage-topic/src/manager/persistence.rs), used by both startup restoration and [dynamic assignment](../crates/remote-storage-topic/src/manager/assignment.rs) | [`proof.json`](../verif/krabka_verified_rlib/remote_metadata/remote_metadata_resume_cursor/proof.json) | Snapshot framing and cache contents remain host assumptions. The host validates every retained per-partition cursor before importing the dump; one invalid cursor discards the whole dump and restores empty-cache/full-replay state. Missing snapshot partitions are padded with `-1`, surplus partitions are ignored, and dynamic assignment calls the same adapter over the validated committed vector. Kafka HWM fetch, assignment delivery, snapshot file I/O, and the cache-before-committed capture ordering remain I/O and concurrency assumptions; failed HWM reads continue to gate `NotReady` and retry through the existing reconciler. |
| [`tiered_earliest_finished_index`](../crates/verified/src/remote_read.rs) selects a valid finished remote segment with the minimum start; [`tiered_latest_finished_index`](../crates/verified/src/remote_read.rs) selects one with the maximum inclusive end; and [`tiered_owning_epoch_index`](../crates/verified/src/remote_read.rs) selects the nonnegative leader epoch with the greatest in-segment start at or below that end. | The broker remote reader's [tiered `ListOffsets` adapter](../crates/broker/src/remote_reader/tiered_offsets.rs) | [`earliest proof`](../verif/krabka_verified_rlib/remote_read/tiered_earliest_finished_index/proof.json), [`latest proof`](../verif/krabka_verified_rlib/remote_read/tiered_latest_finished_index/proof.json), and [`epoch proof`](../verif/krabka_verified_rlib/remote_read/tiered_owning_epoch_index/proof.json) | The host takes one RLMM listing and constructs equal-length parallel arrays without reordering, then maps each proved index back to that same immutable vector. A candidate is valid only when the copy finished and `0 <= start <= end`; negative or inverted metadata is invisible. Epoch keys and starts come from the selected segment's one map, and an absent valid owner fails closed to no tiered latest offset. Equal minimum starts or maximum ends may select either tied segment; correctness assumes tied maximum-end segments describe the same leader lineage. RLMM readiness and listing consistency, lifecycle durability, and concurrent metadata changes after the returned snapshot remain host and I/O assumptions. |
| [`remote_read_relative_offset`](../crates/verified/src/remote_read.rs) admits only finished remote segments whose inclusive range contains the request, whose requested leader epoch owns that exact `[epoch start, next epoch start)` subrange, and whose relative offset fits the sparse index. | The remote-storage [metadata cache](../crates/remote-storage/src/cache.rs) and broker [remote Fetch reader](../crates/broker/src/remote_reader/fetch.rs) | [`proof.json`](../verif/krabka_verified_rlib/remote_read/remote_read_relative_offset/proof.json) | The host derives the requested and next epoch starts from each segment's ordered epoch map; every primary and fallback candidate delegates before index or object access. |
| [`remote_fetch_end_position`](../crates/verified/src/remote_read.rs) computes an inclusive bounded-fetch end without wrapping and uses an open end for zero, exact-segment, or oversized caps. | The remote-storage [`OffsetIndex` range adapter](../crates/remote-storage/src/index/offset.rs) | [`proof.json`](../verif/krabka_verified_rlib/remote_read/remote_fetch_end_position/proof.json) | The host saturates `usize` caps above `u32::MAX` before delegation, which preserves the open-ended result. |
| [`remote_time_index_offset_usable`](../crates/verified/src/remote_read.rs) stops decoding at the first non-increasing relative offset; [`remote_time_index_candidate_count`](../crates/verified/src/remote_read.rs) selects the maximal usable prefix strictly below a target timestamp. | The remote-storage [`TimeIndex` floor adapter](../crates/remote-storage/src/index/time.rs) | [`padding proof`](../verif/krabka_verified_rlib/remote_read/remote_time_index_offset_usable/proof.json), [`selection proof`](../verif/krabka_verified_rlib/remote_read/remote_time_index_candidate_count/proof.json) | The zero-copy parser exposes only complete 12-byte big-endian entries and ignores a trailing partial entry. The adapter decodes each relative offset, delegates the padding decision, and does not decode the terminator's timestamp or inspect later entries. It then maps every accepted timestamp and relative offset exactly. Kafka time-index writers produce nondecreasing timestamps while usable relative offsets strictly increase. The proof covers the decoded sequence after this parser boundary. |
| [`remote_txn_overlap_decision`](../crates/verified/src/remote_txn.rs) classifies inclusive aborted-transaction and Fetch ranges exactly, rejecting either inverted interval. | The remote-storage [transaction-index reader](../crates/remote-storage/src/index/txn.rs), which also rejects payloads not divisible by the 24-byte entry size | [`proof.json`](../verif/krabka_verified_rlib/remote_txn/remote_txn_overlap_decision/proof.json) | The host decodes each big-endian entry field exactly and rejects a torn partial entry before calling the kernel. |
| [`restore_batch_step`](../crates/verified/src/restore.rs) accepts only a non-overlapping archived batch base and computes its exclusive end without overflow, while permitting gaps preserved by Kafka compaction; [`restore_record_coordinates`](../crates/verified/src/restore.rs) validates each record delta and derives its absolute offset and timestamp without overflow; [`restore_record_selected`](../crates/verified/src/restore.rs) keeps exactly records at or below an inclusive offset bound and strictly before an exclusive timestamp bound when no exclusion matches; [`restore_batch_filter_decision`](../crates/verified/src/restore.rs) makes keep, empty, and filter equivalent to the folded record decisions; and [`restore_batch_past_offset_bound`](../crates/verified/src/restore.rs) stops an ordered batch walk exactly when the next base is past its partition bound. | The restore verifier's [record-batch walk](../crates/restore/src/verify/log_walk.rs), the compiled [bound and record filter](../crates/restore/src/bound.rs), and the materializer's [batch preparation](../crates/restore/src/materialize/prepare.rs) and [ordered segment walk](../crates/restore/src/materialize/segment.rs) | [`batch proof`](../verif/krabka_verified_rlib/restore/restore_batch_step/proof.json), [`coordinate proof`](../verif/krabka_verified_rlib/restore/restore_record_coordinates/proof.json), [`record-selection proof`](../verif/krabka_verified_rlib/restore/restore_record_selected/proof.json), [`batch-fold proof`](../verif/krabka_verified_rlib/restore/restore_batch_filter_decision/proof.json), and [`batch-skip proof`](../verif/krabka_verified_rlib/restore/restore_batch_past_offset_bound/proof.json) | Batch framing and CRC are validated first. The host maps each partition's parsed inclusive offset bound and the global exclusive timestamp bound exactly, computes producer, half-open offset-range, UTF-8 key-regex, and header-regex matches before calling the record kernel, and scans verified batches in archive order. A straddling data batch is decoded and filtered rather than skipped. Control batches bypass all per-record predicates, including timestamp, to preserve transaction markers; the host relies on Kafka's one-marker-at-base control-batch shape, and a control batch whose base is past an offset bound is skipped with the ordered tail. Record parsing and coordinate failures are archive-integrity errors before selection or rewriting; target-log append, synchronization, and retry remain I/O assumptions. |
| [`restore_rewritten_batch_header`](../crates/verified/src/restore.rs) admits only owned rewrites with a nonnegative retained count, an exactly representable archived offset frontier, and a legal data, transaction-control, or non-transactional barrier producer tuple; [`restore_rewritten_record`](../crates/verified/src/restore.rs) requires every retained record delta to advance strictly inside that span and reconstructs its absolute offset and timestamp without overflow below the preserved maximum timestamp. | The restore materializer's [owned-batch preflight](../crates/restore/src/materialize/prepare.rs) and [segment accounting and append](../crates/restore/src/materialize/segment.rs) | [`header proof`](../verif/krabka_verified_rlib/restore/restore_rewritten_batch_header/proof.json) and [`record proof`](../verif/krabka_verified_rlib/restore/restore_rewritten_record/proof.json) | The host fully decodes a filtered data batch, preserves survivor order and the archived offset span, derives `records_count` from the retained vector, and checks every retained delta and reconstructed timestamp. Control batches bypass selection but take this owned path so the log reads the marker and applies transaction or barrier semantics; data batches untouched by predicates retain their previously CRC-validated verbatim bytes. The protocol encoder recomputes the count and CRC; preflight validates that fresh framing and compares every admitted header field before either dry-run accounting or mutation, so compressed sizes are counted from exact encoded bytes. The target log reuses the same owned encoder and rolls back failed appends; allocator, compression-codec, filesystem, synchronization, rollback, and crash atomicity remain host assumptions. |
| [`restore_index_frontier`](../crates/verified/src/restore_sidecar.rs) derives the exact representable inclusive relative-offset bound; [`restore_offset_index_entry_valid`](../crates/verified/src/restore_sidecar.rs), [`restore_time_index_entry_valid`](../crates/verified/src/restore_sidecar.rs), and [`restore_txn_index_entry_valid`](../crates/verified/src/restore_sidecar.rs) require strict sparse-index coordinates and verified log bounds; [`restore_leader_epoch_entry_valid`](../crates/verified/src/restore_sidecar.rs) requires nonnegative strictly ordered segment-scoped epochs and starts; and [`restore_producer_ids_strict`](../crates/verified/src/restore_sidecar.rs) makes sorted producer IDs unique. Each producer state also passes [`producer_snapshot_entry_valid`](../crates/verified/src/producer_snapshot.rs) at the segment's exact exclusive frontier. | The restore verifier's [index](../crates/restore/src/verify/index.rs), [leader-epoch](../crates/restore/src/verify/leader_epoch.rs), and [producer-snapshot](../crates/restore/src/verify/snapshot.rs) adapters | [`frontier proof`](../verif/krabka_verified_rlib/restore_sidecar/restore_index_frontier/proof.json), [`offset-index proof`](../verif/krabka_verified_rlib/restore_sidecar/restore_offset_index_entry_valid/proof.json), [`time-index proof`](../verif/krabka_verified_rlib/restore_sidecar/restore_time_index_entry_valid/proof.json), [`transaction-index proof`](../verif/krabka_verified_rlib/restore_sidecar/restore_txn_index_entry_valid/proof.json), [`leader-epoch proof`](../verif/krabka_verified_rlib/restore_sidecar/restore_leader_epoch_entry_valid/proof.json), [`producer-ID proof`](../verif/krabka_verified_rlib/restore_sidecar/restore_producer_ids_strict/proof.json), and the shared [`producer-state proof`](../verif/krabka_verified_rlib/producer_snapshot/producer_snapshot_entry_valid/proof.json) | The host requires complete fixed-width big-endian index entries, preserves each decoded field, and supplies the log byte length and inclusive offset extent established by the verified batch walk. Offset-index coordinates and transaction starts strictly increase; time-index offsets strictly increase while timestamps may repeat. The strict text parser enforces the exact declared leader-epoch row count and the export path's clamped segment bounds. Snapshot framing and CRC are checked before fixed-width decoding; its filename-independent frontier is `verified end + 1`. The host sorts a copy of decoded producer IDs before applying the strict-order proof, so arbitrary entry order is accepted while duplicates are not. Producer timestamps are persisted but are not part of producer sequencing validity. Object listing, fetch stability, byte decoding, sorting, CRC implementation, allocation, and target-side I/O remain host assumptions. Malformed, stale, overflowing, duplicate, or out-of-range sidecars fail before materialization; pure validation is repeatable after uncertain reads. |
| [`restore_archive_reconcile`](../crates/verified/src/restore.rs) classifies each archive-scan and RLMM-snapshot observation: keep scanned live bytes, exclude absent or delete-started bytes, and reject every live-source disagreement or unknown state. | The restore inventory's [archive-source reconciliation](../crates/restore/src/discover/reconcile.rs) | [`proof.json`](../verif/krabka_verified_rlib/restore/restore_archive_reconcile/proof.json) | The host maps the four RLMM lifecycle states exactly and keys segments by `(UUID, start offset)`. Duplicate partition or segment keys are disagreements instead of retries that overwrite prior metadata. Snapshot file I/O and decoding and archive listing consistency remain assumptions. Missing and corrupt snapshots fail before reconciliation. The scan and snapshot must remain stable while this read-only comparison runs. |
| [`schema_failure_decision`](../crates/verified/src/schema.rs) permits unvalidated records only when fail-open is configured and the registry failure is transient. | The broker schema validator's [registry classifier](../crates/broker/src/schema_validation/validator/cache.rs) and [admission path](../crates/broker/src/schema_validation/validator/check.rs) | [`proof.json`](../verif/krabka_verified_rlib/schema/schema_failure_decision/proof.json) | The host classifies transport errors, HTTP 408/429, and 5xx as transient; unknown IDs, other statuses, and malformed successful responses fail closed. |
| [`snapshot_chunk_admission`](../crates/verified/src/snapshot.rs) admits chunks only for one fixed snapshot identity and declared size, at the exact accumulated byte position, within the configured bound; it completes only when the next position equals the declared size and rejects overflow, duplicates, gaps, overlaps, and changed metadata without advancing. | KRaft's follower [`SnapshotFetchState`](../crates/kraft-core/src/snapshot_fetch.rs) | [`proof.json`](../verif/krabka_verified_rlib/snapshot/snapshot_chunk_admission/proof.json) | The host compares the full `(end offset, epoch)` identity, converts exact buffer and chunk lengths to signed wire coordinates, and mutates the latched size and byte buffer only after admission. `BytesMut` allocation and copy success, plus the engine's documented discard of state after `Restart`, remain host and file-I/O assumptions. |
| [`snapshot_install_decision`](../crates/verified/src/snapshot.rs) installs only a nonnegative future snapshot identity when no mandatory downgrade snapshot is pending, classifying equal or older boundaries as stale; [`snapshot_prune_admission`](../crates/verified/src/snapshot.rs) permits pruning only at a nonnegative boundary at or below the committed frontier. | KRaft's follower [snapshot installer](../crates/raft/src/kraft/controller/replication.rs) and leader [checkpoint-prune path](../crates/raft/src/kraft/controller/snapshotting.rs) | [`install proof`](../verif/krabka_verified_rlib/snapshot/snapshot_install_decision/proof.json), [`prune proof`](../verif/krabka_verified_rlib/snapshot/snapshot_prune_admission/proof.json) | The separately proved chunk state supplies the completed requested identity and exact bytes. Snapshot decoding succeeds before durable install. Checkpoint writing must durably persist the exact snapshot bytes and boundary before pruning; filesystem rename, fsync, log install/prune atomicity, image publication, and checkpoint cleanup remain I/O assumptions. The host re-reads the current log end for install and committed HWM for prune immediately before mutation. |
| [`latest_checkpoint_index`](../crates/verified/src/checkpoint.rs) selects a maximum `(end offset, epoch)` id; [`checkpoint_id_newer`](../crates/verified/src/checkpoint.rs) proves the lexicographic order and [`checkpoint_id_retained`](../crates/verified/src/checkpoint.rs) retains exactly the selected full id. | KRaft's [checkpoint directory scanner and cleanup](../crates/raft/src/kraft/controller/checkpoint.rs) | [`selection proof`](../verif/krabka_verified_rlib/checkpoint/latest_checkpoint_index/proof.json), [`ordering proof`](../verif/krabka_verified_rlib/checkpoint/checkpoint_id_newer/proof.json), [`retention proof`](../verif/krabka_verified_rlib/checkpoint/checkpoint_id_retained/proof.json) | The host accepts only UTF-8 filenames that parse and round-trip to the canonical fixed-width `<end offset>-<epoch>.checkpoint` form. Directory enumeration, file reads, atomic temp-file rename, and best-effort deletion remain filesystem assumptions. Malformed names are ignored and never selected or deleted. |
| [`share_prune_frontier`](../crates/verified/src/share.rs) returns the minimum latest-snapshot offset across every supplied live share key, proving both lower-bound safety and the maximal safe prune point. | The share coordinator's [`__share_group_state` pruning adapter](../crates/broker/src/share_coordinator/pruning.rs) | [`proof.json`](../verif/krabka_verified_rlib/share/share_prune_frontier/proof.json) | The caller freezes every live key mapped to one state partition, then supplies each exact `last_snapshot_offset`. A key without a folded snapshot carries the default offset `0`, which pins pruning at the log start. An empty partition returns no frontier. Concurrent state changes after collection may make pruning more conservative; the selected lower bound remains safe. |
| [`site_loss_survivors`](../crates/verified/src/stretch.rs) computes the replica count after one site fails. | `krabka-verified` calls it from `min_insync_is_site_loss_safe`; the [`krabka-broker` stretch model](../crates/broker/src/stretch_cluster_model/config.rs) calls it directly. | [`proof.json`](../verif/krabka_verified_rlib/stretch/site_loss_survivors/proof.json) | `1 <= rf <= 1024`; `1 <= sites <= 1024`. |
| [`min_insync_is_site_loss_safe`](../crates/verified/src/stretch.rs) checks the lower and upper durability bounds for `min.insync.replicas`. | [`krabka-broker`](../crates/broker/src/config/stretch.rs) | [`proof.json`](../verif/krabka_verified_rlib/stretch/min_insync_is_site_loss_safe/proof.json) | `1 <= rf <= 1024`; `1 <= sites <= 1024`. |
| [`quorum_survives_any_single_site_loss`](../crates/verified/src/stretch.rs) checks that each one-site loss leaves a strict voter majority. | The [`krabka-broker` stretch model](../crates/broker/src/stretch_cluster_model/config.rs); no production caller. | [`proof.json`](../verif/krabka_verified_rlib/stretch/quorum_survives_any_single_site_loss/proof.json) | At most 1024 sites; each site has between 0 and 1024 voters. |
| [`plan_consume`](../crates/verified/src/throttle.rs) caps a refill, grants at most the request, and conserves the capped tokens. | [`krabka-throttle`](../crates/throttle/src/runtime/consume.rs) | [`proof.json`](../verif/krabka_verified_rlib/throttle/plan_consume/proof.json) | None. |
| [`transaction_completion_decision`](../crates/verified/src/transaction.rs) permits `EndTxn` completion only for the prepared producer identity and state, treats only the intended completed identity as idempotent, and rejects stale or conflicting state. | The broker's [`EndTxn` reacquisition guard](../crates/broker/src/txn/handlers/end_txn/reacquire.rs) and [completion decision](../crates/broker/src/txn/decision.rs) | [`proof.json`](../verif/krabka_verified_rlib/transaction/transaction_completion_decision/proof.json) | Prepare and complete state tags are distinct; the host maps Kafka transaction states and producer identities exactly into primitives. |
| [`transaction_reaper_completion_decision`](../crates/verified/src/transaction.rs) rejects malformed and stale identities, permits completion only for the exact prepared snapshot, and treats only the intended completed identity as an at-most-once retry. | The transaction coordinator's [idle-reaper completion adapter](../crates/broker/src/txn/coordinator/reaper.rs) and serialized [transaction-state persistence](../crates/broker/src/txn/coordinator/persistence.rs) | [`decision proof`](../verif/krabka_verified_rlib/transaction/transaction_reaper_completion_decision/proof.json) and [`clone proof`](../verif/krabka_verified_rlib/transaction/impl_Clone_for_TransactionReaperCompletionDecision/clone/proof.json) | The host compares the complete persisted `TxnEntry`, including transaction ID, current and staged identities, state, partition set, timeout, and timestamps. It serializes all writes for the transaction-state partition, rejects an entry handle replaced while waiting, and holds the current entry's lock from the recheck through the durable completion append. Prepare and complete changes are staged on clones, so allocation or append failure does not change the live entry. An exact completed retry writes nothing. Existing `PrepareAbort` retries also reject the persisted 2PC no-timeout sentinel. Log append durability, producer-ID allocation, crash atomicity after an append, marker transport, and coordinator ownership remain host or I/O assumptions. |
| [`transaction_marker_materialization_decision`](../crates/verified/src/transaction.rs) rejects malformed identities and stale producer or coordinator generations, suppresses an exact completed retry, and selects committed-offset publication only for a pending COMMIT on `__consumer_offsets`; [`replay_batch_cursor_decision`](../crates/verified/src/recovery.rs) bounds every transactional-offset scan step and rejects overlap, negative deltas, range escape, and signed overflow. | Both local and inter-broker marker fan-out use the broker's shared [marker materialization adapter](../crates/broker/src/txn/handlers/write_txn_markers/materialize.rs), which reads generations from the log's [producer state](../crates/log/src/log/state.rs) and scans offsets through the [bounded adapter](../crates/broker/src/txn/handlers/write_txn_markers/offsets.rs). | [`marker decision proof`](../verif/krabka_verified_rlib/transaction/transaction_marker_materialization_decision/proof.json), [`clone proof`](../verif/krabka_verified_rlib/transaction/impl_Clone_for_TransactionMarkerMaterializationDecision/clone/proof.json), and [`scan-step proof`](../verif/krabka_verified_rlib/recovery/replay_batch_cursor_decision/proof.json) | The host supplies the request PID and epochs, the exact latest producer and coordinator epochs, whether that PID is pending, the decoded marker type, and exact topic identity. A per-partition lock spans admission, append acknowledgement, and any committed-offset actor publication, so conflicting markers cannot both observe one pending generation; an exact completed retry appends nothing. Missing producer state for a pending transaction fails closed. Offset records are decoded only from bounded transactional batches for the admitted PID, aborts and non-offsets partitions never publish, and an append error returns before publication. After a successful COMMIT append, decoded offsets remain in the locked retry backlog until the group actor acknowledges them; a delivery failure leaves that backlog intact, and an exact marker retry drains it without appending another marker. Log append durability, actor delivery, process-crash atomicity, request decoding, and producer-state recovery remain host or I/O assumptions; startup replay independently republishes only transactions closed by durable COMMIT markers. |
| [`transaction_pid_install_decision`](../crates/verified/src/transaction.rs) admits a recovered or newly persisted transaction identity only from its hash-selected state partition, with a nonnegative current PID and epoch, an absent or well-formed staged identity, and no PID owned by another transactional ID. | The transaction coordinator's [staged log replay and publication](../crates/broker/src/txn/coordinator/persistence.rs) and shared [PID-index adapter](../crates/broker/src/txn/coordinator/pid_index.rs) | [`decision proof`](../verif/krabka_verified_rlib/transaction/transaction_pid_install_decision/proof.json) and [`clone proof`](../verif/krabka_verified_rlib/transaction/impl_Clone_for_TransactionPidInstallDecision/clone/proof.json) | The host decodes complete Kafka transaction keys and values, checks the key's exact hash partition, preserves record order inside each sorted local partition, and supplies exact current PID owners. Replay folds into a temporary image: a later value evicts every older PID for only its transactional ID, an exact retry is idempotent, and a tombstone removes both state and all its PID mappings. Missing partitions, read or decode failures, regressing or overflowing batch coordinates, malformed identities, and cross-transaction PID collisions discard the temporary image, latch recovery invalid, and clear coordinator leadership before returning an error. Live publication rechecks PID ownership under one short lock after the durable append. Startup publishes before concurrent readers; producer-ID allocator uniqueness, log append and read durability, decoding, allocation, locking, and process-crash atomicity remain host and I/O assumptions. |
| [`transaction_partition_registration`](../crates/verified/src/transaction.rs) admits a new or idempotent partition registration only at the broker that coordinates the exact transactional ID and for the matching nonnegative producer ID and epoch, with no staged identity and a state that can enter `Ongoing`. | The transaction coordinator's shared [partition-registration adapter](../crates/broker/src/txn/coordinator/registration.rs), called by `AddPartitionsToTxn`, `TxnOffsetCommit`, and the transactional [Produce fence](../crates/broker/src/handlers/produce/producer_checks.rs) | [`proof.json`](../verif/krabka_verified_rlib/transaction/transaction_partition_registration/proof.json) | The host obtains coordinator ownership from the current leader-partition set, looks the entry up by the request transactional ID, compares the stored transactional ID and full producer generation, and checks exact topic-partition set membership. Produce derives the transactional ID only from the persisted PID index and persists the exact produced partition before append; missing and malformed identities fail closed. An exact retry is persisted again, so a prior uncertain registration write cannot turn into an unpersisted append. Transaction-log durability, PID-index integrity, metadata changes after the check, and concurrent registration serialization remain host and I/O assumptions. |
| [`first_unstable_offset`](../crates/verified/src/transaction.rs) selects the minimum pending transaction start, uses log end only when none is pending, and rejects a start beyond log end; [`transaction_marker_closes`](../crates/verified/src/transaction.rs) closes state only for an unambiguous COMMIT or ABORT belonging to a pending producer; [`aborted_transaction_interval`](../crates/verified/src/transaction.rs) admits only an ordered inclusive interval for a live producer; and [`aborted_transaction_overlaps`](../crates/verified/src/transaction.rs) is equivalent to intersection with a nonempty half-open Fetch range. | The local log's owned [transaction marker and LSO path](../crates/log/src/log/transaction.rs), [append path](../crates/log/src/log/append.rs), verbatim [replication path](../crates/log/src/log/verbatim.rs), [recovery replay](../crates/log/src/log/open.rs), and local [aborted-transaction index](../crates/log/src/txn_index.rs) | [`LSO proof`](../verif/krabka_verified_rlib/transaction/first_unstable_offset/proof.json), [`marker proof`](../verif/krabka_verified_rlib/transaction/transaction_marker_closes/proof.json), [`interval proof`](../verif/krabka_verified_rlib/transaction/aborted_transaction_interval/proof.json), [`overlap proof`](../verif/krabka_verified_rlib/transaction/aborted_transaction_overlaps/proof.json) | The host keeps one first offset per nonnegative producer and supplies the exact checked log end. It parses the first control-record key exactly; malformed or unknown control types do not clear pending state, and only the same producer ID can close its range. Broker marker admission supplies producer and coordinator fencing through `transaction_marker_materialization_decision`. The local index decodes only complete 24-byte big-endian entries, rejects invalid intervals, and canonicalizes only fully identical retries. Log, index, snapshot, append, rewrite, and synchronization I/O remain host assumptions; append rollback retains pending state on a sidecar failure, while an uncertain index sync may leave an exact retry record that startup safely canonicalizes. |
| [`next_producer_identity`](../crates/verified/src/transaction.rs) preserves unverified identities, increments normal and recovery epochs within their distinct bounds, and rotates to a fresh producer ID at the fencing boundary. | The broker's [`EndTxn` producer-identity adapter](../crates/broker/src/txn/handlers/end_txn/producer_identity.rs) | [`proof.json`](../verif/krabka_verified_rlib/transaction/next_producer_identity/proof.json) | The host selects recovery mode only for staged recovery identities and supplies a fresh producer ID when rotation is required. |
| [`resolve_transaction_timeout`](../crates/verified/src/transaction.rs) maps KIP-939 transactions to the non-colliding `i32::MAX` sentinel and otherwise clamps the requested timeout; [`should_abort_idle_transaction`](../crates/verified/src/transaction.rs) permits the reaper to abort only an expired, ongoing, non-2PC transaction and fails closed on a backwards clock. | The broker's shared [2PC timeout and reaper adapter](../crates/broker/src/txn/two_pc.rs) | [`timeout proof`](../verif/krabka_verified_rlib/transaction/resolve_transaction_timeout/proof.json), [`reaper proof`](../verif/krabka_verified_rlib/transaction/should_abort_idle_transaction/proof.json) | Configuration establishes `0 < min <= max < i32::MAX`; the host maps only `TxnState::Ongoing` to the kernel's ongoing state. |
| [`future_log_swap_admission`](../crates/verified/src/storage.rs) permits a future-log swap only when the current and future log-end offsets are exactly equal, preventing both truncation and unreplicated suffix promotion. | The broker's [future-log swap guard](../crates/broker/src/partition_writer/swap.rs) | [`proof.json`](../verif/krabka_verified_rlib/storage/future_log_swap_admission/proof.json) | The host holds both log locks and supplies their exact log-end offsets. |
| [`local_truncation_plan`](../crates/verified/src/storage.rs) selects the exact sealed-segment prefix and active-segment disposition at an exclusive cut; [`truncation_relative_offset`](../crates/verified/src/storage.rs) rejects negative, reversed, and `u32`-overflowing relative offsets; [`truncation_batch_retained`](../crates/verified/src/storage.rs) keeps exactly decoded batches ending before the cut; [`truncation_frontier`](../crates/verified/src/storage.rs) clamps dependent frontiers to the actual retained log end. | The local log's [truncation coordinator](../crates/log/src/log/truncate.rs), [segment truncation walk](../crates/log/src/segment/lifecycle.rs), and [transaction-index rewrite](../crates/log/src/txn_index.rs) | [`plan proof`](../verif/krabka_verified_rlib/storage/local_truncation_plan/proof.json), [`relative-offset proof`](../verif/krabka_verified_rlib/storage/truncation_relative_offset/proof.json), [`batch-boundary proof`](../verif/krabka_verified_rlib/storage/truncation_batch_retained/proof.json), [`frontier proof`](../verif/krabka_verified_rlib/storage/truncation_frontier/proof.json) | The loaded log supplies strictly increasing, unique segment bases and valid, ordered, nonoverlapping decoded batches. The host retains one active segment, replays producer state, rewrites the transaction, stamp, and offset indexes, and clamps the last stable offset, delivery watermark, and epoch checkpoint to the actual retained end. Filesystem deletion, rewrite, synchronization, and crash atomicity remain I/O assumptions. The independently reconciled diskless frontier is not local-log state and is not clamped here. |
| [`local_append_coordinates`](../crates/verified/src/storage.rs) admits a batch only at the expected logical frontier and computes its inclusive last offset and exclusive successor with checked arithmetic, excluding gaps, duplicates, negative deltas, and signed overflow. | The local log's owned [append coordinator](../crates/log/src/log/append.rs), verbatim [append coordinator](../crates/log/src/log/verbatim.rs), segment [write path](../crates/log/src/segment/append.rs), and [rollback path](../crates/log/src/log/sync.rs) | [`proof.json`](../verif/krabka_verified_rlib/storage/local_append_coordinates/proof.json) | The host supplies `max(local log end, reconciled diskless frontier)` as the expected logical base; a frontier above the local end represents offsets already reserved by the diskless WAL. Batch encoding and CRC validation precede mutation. Segment bytes and sparse indexes are flushed before a synchronized stamp or transaction sidecar. Any later append or checkpoint failure invokes full tail truncation to restore bytes, the file cursor, indexes, producer and transaction state, visibility frontiers, and epoch state before retry. Individual filesystem calls and rollback can still fail; atomic persistence across separate files and process or host crashes remains an I/O assumption. |
| [`remote_segment_transition`](../crates/verified/src/storage.rs) and [`remote_partition_delete_transition`](../crates/verified/src/storage.rs) admit exactly the legal remote-segment and remote-partition deletion lifecycle edges, including their terminal states. | The remote-storage [segment state](../crates/remote-storage/src/metadata/segment_state.rs) and [partition-delete state](../crates/remote-storage/src/metadata/partition_delete.rs) adapters | [`segment proof`](../verif/krabka_verified_rlib/storage/remote_segment_transition/proof.json), [`partition-delete proof`](../verif/krabka_verified_rlib/storage/remote_partition_delete_transition/proof.json) | The host exhaustively maps each Rust enum variant to the proof tags documented by the kernels; `0` means `CopySegmentStarted` for segments and no prior state for partition deletion. Exhaustive production tests cover every source and destination pair. |
| [`remote_cache_action`](../crates/verified/src/storage.rs) admits only legal remote-segment cache transitions, makes exact same-state updates and already-absent delete-finished tombstones no-ops, permits epoch indexing only for copy-finished state, hides delete-started state, removes delete-finished state, and rejects every missing-state resurrection attempt. | The remote-metadata [primary cache and derived epoch-index adapter](../crates/remote-storage/src/cache.rs) | [`proof.json`](../verif/krabka_verified_rlib/storage/remote_cache_action/proof.json) | The host maps the four lifecycle variants to tags `1..=4`, uses `0` only for a missing UUID, and classifies a same-state retry as exact only when state, event timestamp, broker, and any supplied custom metadata agree. After every admitted state change, it rebuilds the derived epoch map from copy-finished primary entries; deterministic `(epoch, start, UUID)` ordering resolves malformed slot collisions and exposes the next finished candidate after deletion. Seed clears both maps, folds duplicate UUIDs monotonically, and retains delete-finished UUIDs as tombstones for the whole seed pass before one rebuild. Segment construction, snapshot decoding, primary-map locking, and durability remain host assumptions. Lookup additionally rechecks finished state, epoch membership, offset span, and relative-offset width before returning metadata. |
| [`diskless_wal_replay_decision`](../crates/verified/src/diskless.rs) makes keyed WAL-index values authoritative over legacy values and makes keyed tombstones remove either arrival order during cross-partition replay; [`diskless_object_reclaimable`](../crates/verified/src/diskless.rs) permits deletion only after grace has elapsed and the locked projection has no reference. | The broker's diskless [WAL-index projection](../crates/broker/src/diskless/wal_index.rs) and [object reclaimer](../crates/broker/src/diskless/flusher.rs) | [`replay proof`](../verif/krabka_verified_rlib/diskless/diskless_wal_replay_decision/proof.json), [`reclaim proof`](../verif/krabka_verified_rlib/diskless/diskless_object_reclaimable/proof.json) | Event tags are `0 = legacy value`, `1 = keyed value`, and `2 = keyed tombstone`. Equal keyed events share a Kafka partition and retain their log order; temporary tombstone markers cover only cross-partition legacy replay and are cleared after every replay fence is delivered. The reclaimer takes a fresh reference check while holding the projection lock through deletion. Object-store listing, Kafka delivery and compaction, replay-fence publication, and object-store I/O remain host assumptions. |
| [`wal_fetch_admission`](../crates/verified/src/wal.rs) fails closed unless authenticated and claimed nodes agree, the local node leads the placement, the claimant is a voter, and the requested epoch is current or unset. | [`krabka-broker`](../crates/broker/src/wal/quorum/registry.rs) | [`proof.json`](../verif/krabka_verified_rlib/wal/wal_fetch_admission/proof.json) | The host maps the authenticated identity and current placement exactly into primitive node IDs and voter order. |
| [`exact_wal_batch_range`](../crates/verified/src/wal.rs) accepts only an empty exact range or decoded batches whose first base equals the requested start, every checked `last + 1` equals the next base, and final successor equals the target; gaps, overlaps, reordering, inverted batches, and successor overflow fail closed. | The diskless WAL [`exact_batches`](../crates/broker/src/wal/quorum/engine/batches.rs) gate used by source and replica readers | [`proof.json`](../verif/krabka_verified_rlib/wal/exact_wal_batch_range/proof.json) | The host decodes and validates each record batch first, then passes parallel base/last arrays in the original byte order plus the exact requested half-open bounds. |
| [`scheduled_delivery_visible`](../crates/verified/src/delivery.rs) exposes a scheduled batch only when its checked activation deadline is at or before the broker clock and fails closed on invalid uncertainty or overflow; [`delivery_watermark_advance`](../crates/verified/src/delivery.rs) clamps the cached watermark into live log bounds, then advances it monotonically without passing the log end; [`coalesce_delivery_range`](../crates/verified/src/delivery.rs) merges exactly overlapping or adjacent inclusive ranges and returns their maximum end without successor overflow. | The log's [local activation walk and watermark](../crates/log/src/log/delivery.rs), [range fold](../crates/log/src/delivery.rs), and the broker's [remote fetch gate](../crates/broker/src/handlers/fetch/remote.rs) | [`visibility proof`](../verif/krabka_verified_rlib/delivery/scheduled_delivery_visible/proof.json), [`watermark proof`](../verif/krabka_verified_rlib/delivery/delivery_watermark_advance/proof.json), [`coalescing proof`](../verif/krabka_verified_rlib/delivery/coalesce_delivery_range/proof.json) | Configuration supplies a nonnegative millisecond clock-uncertainty bound; local and remote paths use the same broker clock reading and decoded batch `max_timestamp`. Storage supplies valid ordered batch ranges and `log_start <= log_end`. Retention or truncation may first clamp a stale watermark down to the new live range; without a range change, each proved step is monotonic. |
| [`select_wal_voter_index`](../crates/verified/src/wal.rs) returns the first sorted candidate whose node and rack are both unused, additionally requiring the local node for the first placement. | The broker's [diskless WAL placement policy](../crates/broker/src/wal/quorum/placement.rs) | [`proof.json`](../verif/krabka_verified_rlib/wal/select_wal_voter_index/proof.json) | The host sorts brokers by node ID, maps exact rack-string equality to compact IDs, and maps the proved index back to the same candidate list. |

Creusot also creates sessions for logic functions, lemmas, private helper
functions, and generated `Clone` implementations. These sessions support the
contracts in the ledger. They are not additional production kernels.

## Run the Creusot Proofs

The [`.creusot-version`](../.creusot-version) file pins the Creusot release.
That release pins the Rust nightly that `creusot-rustc` needs. Its
`creusot-setup/src/tools_versions_urls.rs` file pins the prover binaries and
checksums. Its `creusot-deps.opam` file pins Why3 and Why3find. The local
[`why3find.json`](../why3find.json) file selects the provers and sets the time
limits, search depth, tactics, and proof-search profile. It does not set a
memory limit.

Install the pinned Creusot release and its provers as the
[`proofs` job](../.github/workflows/ci.yml) does. Then run these commands from
the repository root:

```sh
export PATH="${HOME}/.local/share/creusot/bin:${PATH}"
python3 tools/test_creusot_packages.py
packages=(krabka-verified)
python3 tools/creusot_packages.py "${packages[@]}"
for package in "${packages[@]}"; do
  cargo creusot --package "${package}"
done
```

The package check fails if a workspace package that depends on `creusot-std`
or contains a Creusot contract is absent from the explicit proof list. Each
`cargo creusot` command updates its package directory under `verif/`. A
nonzero exit or an unproved goal fails the CI job. CI checks the proof result,
not a clean Git diff. Generated `.coma` files contain an absolute source path,
so another checkout can change them without a change to the proof result.

## Stateright Model-Check Tier

Stateright enumerates reachable states within explicit bounds. It can find a
counterexample in that state space. It does not prove behavior outside the
bounds. Each model states its bounds and properties in its module comments.

The current model entry points are:

| Area | Model entry points |
| :--- | :--- |
| Broker data and Fetch | [`data_path_model`](../crates/broker/src/data_path_model.rs), [`producer_state_model`](../crates/broker/src/producer_state_model.rs), [`replica_state_model`](../crates/broker/src/replica_state_model.rs), [`fetch_session_model`](../crates/broker/src/fetch_session_model.rs), and [`fetch_visibility_model`](../crates/broker/src/handlers/fetch_visibility_model.rs). |
| Broker failover and placement | [`leader_failover_model`](../crates/broker/src/leader_failover_model.rs), [`reassignment_model`](../crates/broker/src/reassignment_model.rs), [`stretch_cluster_model`](../crates/broker/src/stretch_cluster_model.rs), [`diskless_crash_model`](../crates/broker/src/diskless_crash_model.rs), and [`client_server_failover_model`](../crates/broker/src/client_server_failover_model.rs). |
| Broker quorum WAL | [`quorum WAL model`](../crates/broker/src/wal/quorum/engine/model.rs). |
| Broker groups and transactions | [`classic_state_model`](../crates/broker/src/coordinator/unified/classic_state_model.rs), [`consumer_group_composition_model`](../crates/broker/src/coordinator/unified/consumer_group_composition_model.rs), [`reconciler_model`](../crates/broker/src/coordinator/unified/reconciler_model.rs), [`streams-group reconciliation model`](../crates/broker/src/coordinator/unified/streams/actor/streams_group_model.rs), [`decision_model`](../crates/broker/src/txn/decision_model.rs), [`eos_composition_model`](../crates/broker/src/txn/eos_composition_model.rs), and [`two_pc_model`](../crates/broker/src/txn/two_pc_model.rs). |
| Broker share and break-glass | [`share-group membership model`](../crates/broker/src/coordinator/unified/share/actor/share_group_model.rs), [`share_partition/state_model`](../crates/broker/src/share_partition/state_model.rs), [`break_glass/state_model`](../crates/broker/src/break_glass/state_model.rs), and [`break_glass/cross_spend_model`](../crates/broker/src/break_glass/cross_spend_model.rs). |
| Log | [`compact_model`](../crates/log/src/compact_model.rs) and [`leader_epoch_model`](../crates/log/src/leader_epoch_model.rs). |
| KRaft | [`kraft_model`](../crates/raft/tests/kraft_model.rs). |
| Throttle | [`bucket_model`](../crates/throttle/tests/bucket_model.rs). |

Some models drive a production kernel directly. Other models compose real
decision functions with a small environment. Read the model's `DRIVEN` and
`MODELED` notes before you treat its property as a production guarantee.

The share-group membership model drives the production [`ShareGroupState`](../crates/broker/src/coordinator/unified/share/state.rs) join, leave, and timeout transitions; the production [assignment reconciler](../crates/broker/src/coordinator/unified/share/actor/assignment.rs) and share assignor; the shared member-epoch fence; and the production [snapshot/apply replay adapters](../crates/broker/src/coordinator/unified/share/actor/seed.rs). It exhaustively checks two member IDs, one topic with one or two partitions, four logical timeout ticks, epochs through five, and one crash/replay in every reachable ordering. The pinned run reached 23,084 unique states and generated 164,361 transitions at depth 16, below its depth-64 and one-million-state truncation guards. Properties require unique `(member, topic, partition)` coordinates, bounded assignments for members at the target epoch, nonnegative fenced epochs, and exact durable replay projection; reachability witnesses cover unknown-member, stale, and forward heartbeat rejection, timeout, metadata reassignment, replay, and two-member membership. Cross-member partition overlap remains intentional KIP-932 behavior when members outnumber partitions. Request decoding, actor mailbox delivery, offsets-log append durability, cache publication, wall-clock scheduling, metadata snapshot consistency, and share-state persister I/O remain outside the model. A failed offsets-log append is covered by an adapter test: the actor returns `COORDINATOR_LOAD_IN_PROGRESS`, publishes no partial batch, and relies on durable replay after restart.

The streams-group reconciliation model drives the production [`StreamsGroupState`](../crates/broker/src/coordinator/unified/streams/state.rs) membership, timeout, target-installation, and cooperative active-task transitions; the production [streams assignor](../crates/broker/src/coordinator/unified/streams/actor/reconciliation.rs); the shared member-epoch fence; and the production [snapshot/apply replay adapters](../crates/broker/src/coordinator/unified/streams/actor/records.rs). It exhaustively checks two member IDs, one subtopology with one or two tasks, three logical timeout ticks, topology epochs through two, group epochs through five, and one crash/replay in every reachable ordering. The pinned run reached 43,256 unique states and generated 186,829 transitions at depth 16, below its depth-64 and one-million-state truncation guards. Properties require exclusive current and target active-task ownership, topology-valid assignments, nonnegative fenced epochs, coherent group phases, and exact durable replay projection. Reachability witnesses cover unknown-member, stale, and forward heartbeat rejection; timeout; topology growth; replay; withheld ownership; release; and two-member membership. Request decoding, actor mailbox delivery, offsets-log append durability, cache publication, wall-clock scheduling, metadata-source snapshots, and internal-topic I/O remain outside the model. Adapter tests cover malformed reports, epoch exhaustion, retry, stale and forward epochs, and failed log appends. A failed append returns `COORDINATOR_LOAD_IN_PROGRESS`, publishes no partial batch, and leaves recovery to durable replay after the actor restarts.

The quorum WAL model calls the engine's real record-batch append, replica
sync, acknowledgement, HWM advancement, recovery, truncation, and repair
adapters. It checks three voters, at most two one-record batches, leader epochs
0 through 2, and every ordering of up to eight enabled operations. The safety
properties keep HWM inside the leader log, preserve every previously committed
prefix across transitions, and require a byte-identical quorum at the committed
frontier. Reachability checks cover successful and failed acknowledgements,
leader changes, and divergent replicas. KRaft epoch monotonicity and election
of only a live replica that contains the committed prefix are modeled
preconditions. Successful filesystem calls are atomic operation boundaries;
allocation, fsync, and crash atomicity below those calls remain I/O assumptions.

## Outside Both Tiers

Code that is not in the Creusot ledger or the Stateright inventory is in
neither tier. This includes most protocol encoding and decoding, authorization,
network and async orchestration, file and object-store I/O, and operational
configuration. Unit, property, integration, differential, and container tests
cover those areas. Those tests are evidence, but they are not a Creusot proof
or a Stateright model check.

A proved kernel does not prove its caller. The proof starts after the caller
establishes the preconditions in the ledger. A Stateright model does not prove
the I/O around the decisions that it drives.
